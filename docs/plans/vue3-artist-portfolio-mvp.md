# Artist Portfolio Website - Vue 3 MVP

## Overview

Build a customisable, single-artist portfolio website for an **actor-musician** using Vue 3 + TypeScript, hosted statically on AWS (S3 + CloudFront), with automated deployment via GitHub Actions.

The purpose (from README): allow artists to host their own portfolio website without paying for Squarespace-style services. A technically-minded person sets up the project once (deploy infra, connect GitHub); after that, the artist customises everything themselves through a browser-based editor with **no development skills and no AWS backend**.

Customisation works via **Sveltia CMS**, a Git-based CMS served at `/admin`: the artist edits content (bio, contact info, headshots, photos, videos, theme colours/fonts) and uploads images in a web UI; changes commit directly to the GitHub repo and the CI pipeline rebuilds and redeploys the site (~1–2 min publish delay). Videos are supported by embedding YouTube/Vimeo URLs - nothing is self-hosted. A draft preview mode was designed but is **deferred to post-v1** (see Future Enhancements).

**In scope (v1):** Home with Actor/Musician headshot tiles, Actor page (hero, showreel videos, headshots carousel, portrait gallery), Musician page (hero, intro, awards, highlight/project videos, gallery), About Me, Contact (links + mailto, no form backend), theme customisation (colours/fonts via CMS), Terraform infrastructure, GitHub Actions CI/CD, documentation for both the technical setter-upper and the non-technical artist.

**Out of scope (v1):** multi-artist tenancy, contact-form backend, custom domain/Route53/ACM, blog, e-commerce, SSR/SEO-heavy rendering optimisations, draft preview mode (deferred - see Future Enhancements), and - critically - **any form of layout composition**: artists edit content and theme only; page structure is fixed in code. No section/page builders (that is Squarespace's product, not this project's). Future flexibility comes from config toggles and layout presets on fixed slots, never from free-form composition.

**agent notes** do not run playwright/e2e tests unless requested as they are slow. Always run the minimum tests/build required. **Never create SVG/placeholder image files unless the user explicitly asks.**

### Site structure (final design, locked 2026-08-22)

The artist works as both actor and musician; the site splits into two halves reached from Home.

- **Nav (all pages):** Home · About Me · Contact - Actor and Musician are deliberately **not** nav items.
- **Home (`/`):** artist name/tagline (from site settings) and two headshots side by side linking to `/actor` and `/musician`.
- **Actor (`/actor`):** fullscreen hero image → "Actor" heading with embedded videos → "Gallery" heading containing Headshots (sideways carousel) and Gallery Images (portrait grid, titles below) → link to Musician.
- **Musician (`/musician`):** fullscreen hero image → "Musician" heading + intro text → Awards (title, text, image) → Highlights (videos with descriptions) → Original Projects (videos) → Gallery (pictures with descriptions) → link back to Actor.
- **About Me (`/about`)** and **Contact (`/contact`)** as previously planned.
- Page skeletons are hardcoded in Vue; **all images and text come from CMS content**, with graceful fallbacks when fields are missing.

## Future Enhancements (post-v1)

Ordered roughly by expected value:

1. **Draft preview mode** - enable Sveltia's editorial workflow (saves become draft commits/PRs instead of direct publishes) and add a `?preview=<branch>` override in the content loaders that fetches draft files from `raw.githubusercontent.com`, falling back to bundled content. Gives the artist full-fidelity preview of unpublished text/images/colours on the real site with zero extra AWS infrastructure. Verify at build time: editorial-workflow + PAT compatibility and raw.githubusercontent CORS/rate limits.
2. **Front-end theme switcher** - visitor-facing theme picker cycling through `THEME_PRESETS` (persisted to localStorage); registry and CSS-variable plumbing already in place from task 3.
3. **Layout toggles & presets** - config-driven section visibility ("show commissions section") and 2–3 gallery layout options. Config knobs on fixed slots; never free-form composition.
4. **Contact form backend** - API Gateway + Lambda + SES replacing the mailto link.
5. **Custom domain** - Route53 hosted zone + ACM certificate added to the existing Terraform.
6. **Multi-artist tenancy** - only if demand appears; current design deliberately excludes it. If this becomes a maintained template distributed to several artists, split content into a private per-artist repo at that point (move `content/` + uploads, add cross-repo checkout and a content-commit→deploy trigger to the workflow).

## Architecture Summary

- **App:** Vue 3 + TypeScript + Vite SPA. Dependencies kept minimal: `vue`, `vue-router`, plus `swiper` for image carousels. State via composables (no Pinia needed at this scale).
- **Content:** JSON files under `content/` (settings/, pages/, actor/, musician/), edited by Sveltia CMS, imported at **build time** via `import.meta.glob`. Fully static output.
- **Images:** uploaded via CMS to `public/images/uploads/`, committed to repo, served through CloudFront.
- **Videos:** embedded YouTube/Vimeo URLs, rendered as iframes inline in the Actor/Musician sections. No video is self-hosted and the lightbox handles images only.
- **Admin:** Sveltia CMS at `public/admin/` (index.html + config.yml matching the content schema). Auth via GitHub personal access token entered once by the artist.
- **Infra:** private S3 bucket + CloudFront (Origin Access Control) defined in `infra/` with Terraform. Default `*.cloudfront.net` certificate. Deploy role via IAM OIDC trust with GitHub Actions.
- **CI/CD:** `.github/workflows/` - CI on PRs (lint, typecheck, tests), deploy on push to `main` (build, sync to S3, invalidate CloudFront).

### Proposed repository layout

```
src/                  # Vue app (components/, views/, composables/, router/, styles/)
content/              # CMS-managed content (settings/, pages/, actor/, musician/)
public/images/uploads/# CMS-uploaded media
public/admin/         # Sveltia CMS entry point + config.yml
infra/                # Terraform configuration
.github/workflows/    # CI + deploy pipelines
tests/e2e/            # Playwright specs
docs/                 # ARTIST_GUIDE.md etc.
```

## Tasks

- [x] 1. Scaffold the Vue 3 + TypeScript project
  - [x] Create Vite + Vue 3 + TS app in repo root (respect existing README/.gitignore/LICENSE)
  - [x] Add ESLint (flat config, Vue + TS plugins), Prettier, `vue-tsc` typecheck script
  - [x] Add Vitest + @vue/test-utils, wire `npm run test`; add Playwright skeleton with one trivial passing spec
  - [x] Set up directory structure (components/views/composables/router/content), base global stylesheet, vue-router with placeholder routes for all four pages
  - [x] Verify: dev server runs, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build` all pass
- [x] 2. Build the content layer
  - [x] Define TS types + loader composables for: site settings (name, tagline, social links, theme), home content, about page, contact page, artwork entries
  - [x] Load `content/**` at build time with `import.meta.glob`; provide sensible fallback defaults when files/fields are missing
  - [x] Seed `content/` with realistic example data (incl. one video artwork entry) and placeholder images
  - [x] Unit tests for loaders incl. missing-content fallback behaviour
- [x] 2b. Restructure content layer for the actor–musician design (2026-08-22 redesign)
  - [x] Replace the artworks model with actor/musician types + defaults + normalizers (home headshots, actor page + videos/headshots/gallery, musician page + awards/highlights/projects/gallery)
  - [x] Replace loaders: `useHomePage`, `useActorPage`, `useActorVideos`, `useHeadshots`, `useActorGallery`, `useMusicianPage`, `useAwards`, `useHighlights`, `useProjects`, `useMusicianGallery`
  - [x] Routes: drop `/gallery`; add `/actor` + `/musician` with placeholder views; nav placeholder shows Home/About Me/Contact; update router tests
  - [x] Reseed content with an actor–musician persona: new folder collections, reuse kept placeholder SVGs in fitting slots, delete obsolete ones (create no new images)
  - [x] Rewrite loader unit tests incl. fallback behaviour; verify lint/typecheck/test/build
- [x] 3. Implement the theme system
  - [x] Map theme config to CSS custom properties applied at app root (`--color-*`, `--font-heading/body`), driven by a named-preset registry (`THEME_PRESETS` in `src/composables/theme/`)
  - [x] Verify contrast/responsive behaviour across presets; unit test the theme-to-CSS-variable mapping - contrast verified with WCAG ratio unit tests (AA: text/primary ≥ 4.5, accent ≥ 3 vs background); responsive behaviour is preset-independent since themes emit only custom properties (one preset exists today: "chocolate truffle")
  - [x] Revise palette model per user correction: four equal colour categories (primary / secondary / surface / ink) instead of three roles + fixed text; text is auto-derived per category (AA-gated, see decisions)
- [x] 4. Build shared layout and navigation
  - [x] Header with site name + nav links (Home, About Me, Contact only), footer with social links + copyright; active-route styling; responsive mobile menu
  - [x] Component tests for nav rendering and mobile toggle
- [x] 5. Build the Home view
  - [x] Artist name + tagline from site settings; two headshot tiles side by side linking to /actor and /musician
  - [x] Component test: renders CMS-driven headshots, handles missing images gracefully
- [x] 6. Build the Actor view
  - [x] Fullscreen hero image; "Actor" heading with video embeds
  - [x] "Gallery" heading: headshots carousel (sideways prev/next, keyboard accessible) + portrait gallery grid with titles below
  - [x] Cross-link tile/section to the Musician page
  - [x] Component tests: sections render from content; graceful handling of missing images/videos
- [x] 7. Build the lightbox viewer
  - [x] Lightweight custom lightbox (no external modal library): fullscreen overlay for gallery images with prev/next and captions
  - [x] Keyboard navigation (arrows, Esc), click-outside close, body scroll lock; image-only (videos are inline embeds)
  - [x] Component tests: open/close, navigation, caption rendering
- [x] 8. Build the Musician view
  - [x] Fullscreen hero image; "Musician" heading + intro text
  - [x] Awards section as **one text block + two picture slots** (redesigned per user request, 2026-08-22 — replaced the per-award list); Highlights (videos with descriptions); Original Projects (videos); Gallery (pictures with descriptions)
  - [x] Cross-link back to the Actor page
  - [x] Component tests for all sections
- [x] 9. Build About Me and Contact views
  - [x] About: CMS heading, portrait photo, bio paragraphs, optional statement (quote block)
  - [x] Contact: CMS heading, email address, optional phone (`tel:` link), `mailto:` enquiry button, social links; note in UI copy that replies go to the artist's email (CMS note overrides a hardcoded default line)
  - [x] Component tests for both views
- [x] 9b. Design system & motion pass ("deslop", 2026-08-22)
  - [x] Behaviour: sticky blurred header with scrolled elevation; router `scrollBehavior` smoothly returns to top on every navigation
  - [x] Typography: Fraunces (display/headings) + Newsreader (body) self-loaded via Google Fonts; `classic` font-pairing preset remapped to them
  - [x] Atmosphere: paper-grain + warm-glow overlay, `color-mix` derived tokens (`--surface-veil/deep`, `--ink-band`, `--accent-wash`, `--shadow-lift`), accent-washed gallery bands, tinted imagery
  - [x] Views restyled around existing class names: Home playbill lockup + framed tiles, Actor/Musician hero title overlays with letterspaced captions, ruled video separators, editorial About (offset portrait frame, pull-quote statement) and Contact (framed photo, panelled details), ink-band footer with icon socials, video frames, lightbox polish
  - [x] Motion: motion-v `AnimatePresence` route transitions (`mode="wait"`) wrapped in `MotionConfig reduced-motion="user"`; motion-v staggered Home entrance; CSS micro-interactions (nav underlines, tile zooms, arrow slides); staggered mobile-modal link reveal; global `prefers-reduced-motion` guard
  - [x] PrimeVue pinned to last MIT release (primevue@4.5.5 + @primevue/themes@4.5.4): custom "Ink & Amber" Aura preset, themed Contact enquiry Button, ScrollTop component
  - [x] Follow-up tweaks: cross-links simplified to "Musician →" / "← Actor" pointing at each other; Swiper frame mat recoloured from near-white mix to `--surface-deep`; optional `contactImage` field + framed contact photo
  - [x] Full verification green (127 unit tests)
- [x] 10. Integrate Sveltia CMS
  - [x] Add `public/admin/index.html` (Sveltia via unpkg CDN) + `config.yml` whose collections/schema exactly match the content layer types (settings, pages, actor videos/headshots/gallery, musician highlights/projects/gallery; media folder `public/images/uploads`)
  - [x] Verify locally against seeded content: 11 js-yaml unit tests assert every collection/file/field matches the TS types (and that no awards collection exists); production preview served `/admin/` + `/admin/config.yml` with HTTP 200
  - [x] Interactive Sveltia local-repo walkthrough of the editor UI (needs a human browser session — File System Access API; see docs/CMS_SETUP.md §Editing locally)
  - [x] Document GitHub fine-grained PAT setup for the artist's login (`docs/CMS_SETUP.md`)
  - [x] Smoke checks that `/admin` serves and config parses: covered by the preview HTTP checks + YAML parse tests; Playwright spec added at `tests/e2e/admin.spec.ts` (not run per standing plan note)
- [x] 11. Define AWS infrastructure with Terraform
  - [x] `infra/`: versioned private S3 bucket (public-access blocked, BucketOwnerEnforced, AES256), CloudFront distribution with Origin Access Control, Managed-CachingOptimized cache policy + custom security response-headers policy, bucket policy limited to the distribution via `AWS:SourceArn` condition
  - [x] IAM OIDC provider (token.actions.githubusercontent.com) + least-privilege deploy role trusted only by `timviney/artist-portfolio@refs/heads/main` — s3 List/Get/Put/Delete on the bucket + cloudfront CreateInvalidation on the distribution, nothing else
  - [x] Outputs: bucket name, distribution ID, distribution domain + URL, deploy role ARN
  - [x] Validate: `terraform fmt` ✓, `terraform validate` ✓, `terraform plan` ✓ (12 to add, 0 change, 0 destroy). **Apply is left to the user** per plan note — resources are not provisioned yet
- [x] 11b. Expand theme presets (user request, 2026-08-23)
  - [x] Add five named presets from user-supplied unordered colour lists, working out primary/secondary/surface/ink assignment per colour: salt and pepper, tropical punch, yacht club, lavender fields, stormy morning
  - [x] Register every preset in the Sveltia theme select; admin-config test now asserts exact registry ↔ CMS-options parity
  - [x] Registry-wide tests: AA contrast for all on-colours of all presets, six-digit-hex validation, per-preset palette/fontPairing pins
  - [x] Fix theme application level (user bug report: "background and footer stuck at chocolate truffle"): theme variables now applied to `document.documentElement` via `applyThemeToDocument()` instead of an inline style on the app-shell div - body backgrounds, the grain/glow overlay and all `:root`-declared derived tokens (`--ink-band`, `--accent-wash`, `--surface-*`, `--shadow-lift`) follow the active preset; verified by live preview probe (yacht club: body rgb(242,240,239), footer band from #733E24)
  - [x] Bridge PrimeVue button colours to the active theme (user follow-up on hardcoded Ink & Amber): `.contact-button` overrides the ten `--p-button-primary-*` design tokens with site theme variables; probe confirms navy bg + on-primary text under yacht club
  - [x] Seeded-theme loader test made preset-agnostic (asserts a known registry selection) since the artist flips `theme.json` via CMS
  - [x] Verify lint/typecheck/unit tests/production build + live preview probes
- [x] 11c. Header "Download CV" button + CMS-uploadable CV (user request, 2026-08-23)
  - [x] Feasibility check per user's stop condition: Sveltia media library accepts any file type incl. PDFs and the `file` widget uploads/picks through the browser UI - possible, no stop
  - [x] Optional `cv` field on `SiteSettings` (type/normalizer); no seed value so the button stays hidden until the artist uploads one
  - [x] Sveltia site.json gains a required:false `file` widget labelled "CV (PDF download shown in the header)"; adminConfig spec pins it
  - [x] SiteHeader: `.site-nav__actions` wrapper groups the CV pill with the social icons - desktop absolute top-right (pill sits just inside the socials' corner spot), mobile modal stacks pill above the icon row; `download` attribute on the link; hidden entirely when unset
  - [x] Align tests + `:root` pre-hydration defaults with the user's own hand-tuned palettes from commit c610379 (four surfaces + five secondaries changed; hex-format test now case-insensitive)
  - [x] Verify lint/typecheck/unit tests/build + live preview probe (desktop top-right placement beside socials, mobile modal presence, download attr)
  - [x] Seed a default CV per user request: generated a valid 1.6 KB placeholder `max-rivera-cv.pdf` (scripted minimal PDF 1.4 with correct xref offsets, clearly labelled as a placeholder listing replacement instructions) in the media folder; `site.json` seeds `cv` pointing at it; header tests updated (seeded render case + explicit empty-mock hide case); probe confirms HTTP 200 application/pdf serving and the button on desktop + mobile modal
- [x] 11d. Migrate real artist content from maxyoungacts.com (user request, 2026-08-23)
  - [x] Scrape all five pages of the brother's Squarespace site (home, /home/actor, /home/musician, /aboutme, /contact) - markdown for copy/images, raw HTML for embed URLs (embedly-wrapped YouTube, data-html Vimeo titles)
  - [x] Resolve every embed: oEmbed lookups for YouTube titles; Vimeo titles from iframe attributes (Young & Furey 1/2, Into the Blue)
  - [x] Download 40 assets into public/images/uploads/ (~30 MB): real CV PDF (replaces generated placeholder), 7 headshots, heroes, home tiles, 17 actor gallery images, 2 awards photos, about portrait, contact still, 8 musician gallery images (?format=2500w CDN variants)
  - [x] Rewrite all content JSONs faithfully (ASCII-normalised quotes): Max Young persona, agency contact (agents@mntalent.co.uk + phone + agency note), Who Am I? bio, awards/Eat The Rich text, 2 actor videos, 9 projects incl. Young & Furey collabs; dateAdded stamps preserve exact source display order
  - [x] Flag (see Progress Log): 6 videos are Squarespace-native self-hosted files with no public platform URL and are NOT migrated - actor: Ken's Red speech, Kin, Henry V scene; musician: Drum Reel, Overcome, Overcome live extract (leaves Highlights empty). Needs YouTube/Vimeo uploads + CMS entry, or future component extension for direct video files
  - [x] Delete superseded placeholder SVGs + fake CV; align all seed-pinned unit tests (36 failures resolved); update e2e expectations (not run per standing note)
  - [x] Verify lint/typecheck/tests/build + live preview probe (all counts, hero pixel-load, CV 200/application-pdf)
- [x] 11e. Restage the artist as "Max Pavlovsky" (user request, 2026-08-23)
  - [x] Scope confirmed with user: display name changes everywhere; duo billing "Young & Furey" stays; Instagram handle @maxyoungacts stays; internal filenames/slugs ARE renamed
  - [x] Update `content/settings/site.json` name + the three project descriptions crediting "original music by Max Young"
  - [x] Rename personal media files (`maxyoung-*.jpg` -> `maxpavlovsky-*.jpg`, `max-young-cv-2026.pdf` -> `max-pavlovsky-cv-2026.pdf`) and every content reference incl. headshot entry slugs
  - [x] Align unit tests + e2e expectations pinning "Max Young"
- [x] 11f. Move remaining hardcoded copy into CMS-editable fields (user request, 2026-08-23)
  - [x] Audit views/components for user-facing hardcoded strings (nav labels, tile labels and error-page copy stay structural by design)
  - [x] Add optional fields + defaults + normalizers + seeds + Sveltia widgets: actor hero caption ("Stage & screen"), musician hero caption ("Cello - Guitar - Song"), about eyebrow ("The person behind the work"), contact eyebrow ("Bookings & enquiries"), contact enquiry button label ("Enquire by email")
  - [x] Wire fields into views; update component tests + adminConfig spec
- [x] 11g. Inline links in captions/titles/descriptions (user request, 2026-08-23)
  - [x] Build a tiny safe link renderer: `[label](url)` syntax, HTML-escaped output, http/https/mailto scheme allow-list, plain-text stripper for alt attributes
  - [x] Apply to actor-video descriptions, highlight/project descriptions, actor gallery titles, musician gallery descriptions, and lightbox captions
  - [x] Switch those Sveltia fields to the markdown widget so the artist gets a link toolbar; hint documenting the syntax
  - [x] Unit + component tests incl. XSS cases (scheme blocking, raw HTML escaping)
- [x] 11h. Home screen design tweaks (user request, 2026-08-23)
  - [x] Tiles keep a portrait shape on wide screens (aspect-ratio instead of the viewport-clamped height that reads square at ~44rem tile width)
  - [x] Tile labels move to the top corners: "Actor" top-left of its tile, "Musician" top-right of its tile; scrim gradient flipped to darken the top for label legibility
  - [x] Header site name: remove italics and increase size slightly
  - [x] Update HomeView tests + verify lint/typecheck/unit tests/build + live preview probe
- [ ] 12. Create GitHub Actions workflows
  - [ ] `ci.yml`: lint + typecheck + unit tests (+ e2e) on pull requests
  - [ ] `deploy.yml`: on push to `main` - install, lint, test, build, assume OIDC role, sync `dist/` to S3 with correct cache headers, create CloudFront invalidation
  - [ ] Document required repo variables/secrets (AWS region, role ARN, bucket name)
- [ ] 13. Write documentation
  - [ ] Rewrite README: what it is, architecture overview, developer quickstart, one-time setup/deploy guide (Terraform apply, GitHub secrets, CMS/PAT setup)
  - [ ] Add `docs/ARTIST_GUIDE.md`: non-technical walkthrough - logging into /admin, editing each section (headshots, videos, awards, galleries), uploading images, embedding a video, changing colours/fonts, publishing expectations (1–2 min delay)
- [ ] 14. Final verification pass
  - [ ] Full suite green: lint, typecheck, unit tests, e2e, production build
  - [ ] Manual smoke checklist across all five routes (/, /actor, /musician, /about, /contact) at desktop + mobile widths, including carousel, lightbox and an embedded video
  - [ ] Confirm `npm run preview` serves the built site correctly (SPA fallback caveat noted below)

## Current Unknowns

- [x] Resolved: editorial workflow behaviour with PAT authentication - moot for v1; draft preview mode was deferred to post-v1 (Future Enhancements #1), where verification is planned before building.
- [x] Resolved: SPA client-side routing on CloudFront - the distribution defines `custom_error_response` blocks mapping 403/404 to `/index.html` with HTTP 200 and a 60s error TTL, so vue-router resolves deep links client-side (decision recorded below).
- [ ] Unknown: whether GitHub-hosted runners + S3 transfer costs stay within free tier for typical usage - expected yes for a portfolio-scale site; revisit if usage grows.
- [x] Resolved: visual design direction - settled as the "Ink & Amber" theatrical-playbill aesthetic during the 2026-08-22 design pass (Fraunces/Newsreader, chocolate-truffle palette, grain/rule/framed presentation).

## Decisions

| Decision | Rationale |
|---|---|
| Single artist per deployment | User confirmed; avoids auth/multi-tenancy complexity; each artist deploys their own copy. |
| Git-based CMS (Sveltia) instead of a backend | User wants zero-backend ideally; Sveltia gives non-technical users a real editor UI while keeping infra to static hosting only. |
| Sveltia over Decap | Comparable schema/config model but better editor UX and no OAuth-proxy infrastructure required. |
| Sveltia auth via GitHub PAT ("Sign In with Token") | Verified against official docs: zero server-side setup; token page opens with required scopes pre-selected. Upgrade path exists (Sveltia CMS Authenticator OAuth client) if needed later. |
| S3 + CloudFront with Origin Access Control | User choice; cheap, fast, private origin; default CloudFront certificate for v1. |
| Terraform for IaC | User choice; repeatable infra in `infra/`. |
| GitHub Actions with OIDC deploy role | User choice; no long-lived AWS keys stored in GitHub. |
| Content imported at build time (static) | Site remains purely static and fast; publish delay accepted by user. |
| Vue 3 + TypeScript + Vite; vue-router only, no Pinia | User chose TS; app state is simple enough for composables - fewer dependencies. |
| Videos via YouTube/Vimeo embed URLs only | Avoids video hosting/storage entirely; matches "no backend" goal. |
| Custom lightweight lightbox | Avoids pulling a modal/UI library for one well-understood component. |
| Contact page = links + mailto in v1 | Keeps v1 backend-free; form backend (Lambda + SES) recorded as future enhancement. |
| Vitest + Vue Test Utils; Playwright for one e2e smoke spec | Standard Vue ecosystem tooling; e2e kept deliberately small. |
| CloudFront default URL in v1 | User choice; Route53/ACM added later behind existing Terraform. |
| Fixed page structure; artists edit content + theme only | Avoids rebuilding Squarespace. Layout lives in code; all customisation is data (content, colours, fonts, toggles). Future flexibility = config knobs on fixed slots, not composition. |
| Draft preview mode deferred to post-v1 | User decision: v1 ships without it. The designed approach (editorial workflow + `?preview=<branch>` fetching raw GitHub content, zero extra infra) is preserved in Future Enhancements for the follow-up session. |
| Single **public** repo holding both code and content | User wants the project visible as portfolio work. Repo content equals site content (already public); public repos get unlimited Actions minutes; the Sveltia PAT is required for writes regardless of visibility. Two-repo split still adds cross-repo checkout + deploy triggering for no v1 benefit; documented as a future migration if template-distribution ever happens. Known trade-off: post-v1 draft branches would be publicly browsable pre-publish. Never commit secrets - AWS access uses OIDC roles only. |
| Node upgraded to 24 LTS on dev machine (user-approved) | Latest stack requires it: vue-router 5 needs vite ≥7.3, and vite 7.3+/8 + jsdom 27+ require Node ≥22.12 (dev box had 22.8). CI runners already use current Node. |
| TypeScript pinned to ^6.0.x (not 7) | TS 7 is the new native compiler and removed `lib/tsc`; vue-tsc@3.3.11 cannot drive it yet (`ERR_PACKAGE_PATH_NOT_EXPORTED`). Revisit pin when a TS7-compatible vue-tsc ships. |
| ESLint 10 flat config: plugin-vue `flat/essential` + @vue/eslint-config-typescript; Prettier owns formatting | `flat/essential` avoids stylistic template rules that conflict with Prettier. Verified working combo: eslint@10.9, eslint-plugin-vue@10.10, @vue/eslint-config-typescript@14.9. |
| Latest majors adopted: vite@8, vitest@4, jsdom@30, vue-router@5, @playwright/test@1.62 | User requested latest compatible installs; full toolchain verified green on this set. Vitest config lives in separate `vitest.config.ts` sharing the `@` alias with `vite.config.ts`. |
| Content stored as JSON files (not Markdown) | Structured fields map 1:1 to Sveltia widgets and TS types; avoids adding a markdown parser dependency for v1's fixed layout. Long-form text is arrays of paragraph strings (`bioParagraphs`). |
| Content layer lives in `src/composables/content/` (types / defaults / normalize / loaders) | Keeps planned `composables/` structure; normalizers are pure functions over `unknown` so missing-file/missing-field fallbacks are unit-testable without mocking `import.meta.glob`. Loaders are non-reactive plain functions (content is static at build time). |
| Artwork slugs derived from filenames (`artworks/<slug>.json`) | Single source of truth, CMS-friendly (Sveltia uses filename as identifier); titles fall back to a prettified slug when absent. |
| Font pairing presets defined in the content type: `classic \| modern \| editorial \| playful` | Lets seed data validate against the real preset list now; task 3 maps each preset to actual font stacks/CSS variables. Unknown values fall back to `classic`. |
| Placeholder media committed as hand-made SVGs in `public/images/uploads/` | No offline source of real artwork photos; lightweight, diff-friendly, artist replaces them via the CMS upload flow. Seed contact email uses `.example` domain to avoid pointing at a real address. |
| **Redesign (2026-08-22): actor–musician dual portfolio** | User's final design: nav = Home · About Me · Contact only; Home shows two headshot tiles linking to `/actor` and `/musician`; generic Gallery route + artworks collection removed. Page skeletons stay hardcoded in Vue; all images/text come from CMS content with graceful fallbacks. |
| One Sveltia folder collection per page section (`actor/{videos,headshots,gallery}`, `musician/{highlights,projects,gallery}`) | Each editable section maps to exactly one CMS collection - clearest UX for a non-technical artist; filename becomes the slug. Exception: the musician Awards block lives in `pages/musician.json` (single text + two fixed picture slots, no list to order). |
| List entries sort newest-first by an optional `dateAdded` ISO datetime (CMS label **"Date added"**), ties broken alphabetically by slug; undated entries sink to the end | Replaced the manual `order` number per user request: hand-numbering breaks down as lists grow, while "latest at the top" matches how a portfolio grows and stays editable - change a date to reshuffle. Sveltia's datetime widget pre-fills now on creation. Seed timestamps were chosen to preserve the exact pre-existing display order (including the user-added `making-sounds.json` gallery entry discovered mid-migration). |
| Section headings live in page JSON with hardcoded code fallbacks | Structure is fixed in code but wording stays artist-editable; missing fields render sensible defaults ("Actor", "Gallery", "Awards", …). |
| Headshots = inline sideways carousel; gallery grids open an image-only lightbox | User choice. Videos are inline embeds in the new design, so the lightbox drops its planned video branch. |
| Existing placeholder SVGs partially reused; no new image files created | User instruction: reuse existing placeholders where they fit (portrait → headshots, scenic images → heroes/galleries), delete the rest, and never create SVGs unless explicitly asked. Seed content ships with some slots intentionally empty to exercise graceful-missing behaviour. |
| Themes are named presets; `content/settings/theme.json` stores only `{ "preset": "<name>" }` | User wants themes as selectable options. A code registry (`THEME_PRESETS`, keyed by name with display labels) is the single source of palettes + font pairings; unknown/missing selections fall back to the default preset. The same registry will drive the Sveltia select widget (task 10) and any future front-end switcher. Per-colour editing is deferred until needed. |
| "Chocolate Truffle" four-category palette: primary #713600, secondary #C05800, surface #FDFBD4, ink #38240D | User-supplied colours as four equal categories (Figma-style token grouping) instead of three roles + fixed text: primary = main brand (links/buttons/headings), secondary = accent/support (highlights, hovers, tags), surface = page/card backgrounds, ink = darkest (dark surfaces, strong emphasis). |
| Text colour auto-derived per category (`resolveOnColors`): palette-coherent candidate if it meets AA 4.5:1, else the higher-contrast of white/black | User requirement: "text changing to whatever fits that colour best". Result for chocolate truffle: on-primary = cream (8.7:1), on-secondary = black (4.6:1 - computed; black edges white 4.53:1 on caramel), on-surface = espresso (12.0:1), on-ink = cream (12.0:1). Every pairing is unit-tested ≥ AA. |
| Border colour derived from text at 15% alpha; each preset bundles a font pairing | Keeps presets to the four user-facing colours; `--color-border` = rgba(text, 0.15) so borders stay coherent in every future theme. "Chocolate truffle" ships with the `classic` pairing (serif headings / system sans body); other pairings already defined for future presets. |
| CSS variables applied on `document.documentElement` at startup via `applyThemeToDocument()` (revised 2026-08-23 from the original app-root `:style` binding) | Pure-function mapper (`themeToCssVariables`) stays unit-testable; stylesheet `:root` defaults mirror the default preset for pre-hydration paint. Revision reason: binding on the app-shell div left every *ancestor* (body background) and every `:root`-declared derived token (`--ink-band` etc. - custom properties substitute var() refs at the declaring element) frozen on the default palette, so switching presets changed almost nothing outside the shell. Applying inline on `<html>` overrides the `:root` declarations for base colours, recomputes derivations, and reaches body/overlay/footer. |
| Route table exported as `routes` from `src/router/index.ts`; components/tests create their own routers from it | Lets component tests build isolated memory-history routers (no singleton state leakage between tests) while `main.ts` keeps the single default-exported web-history router; one source of truth for route definitions. |
| `toEmbedUrl()` converts watch/share/shorts URLs to privacy-enhanced embeds (`youtube-nocookie.com`, `player.vimeo.com`); unrecognised URLs render no iframe | Artists paste ordinary YouTube/Vimeo page links in the CMS; conversion happens at render time so content stays human-friendly. youtube-nocookie avoids tracking cookies; unknown URLs fail soft (empty slot) instead of rendering a broken iframe. Lives in `src/composables/video.ts`, shared with the Musician view. |
| Swiper adopted for the headshots carousel (user-requested premade solution, 2026-08-22 tweak pass) | User asked for something off-the-shelf to "flick through images"; Swiper is the Vue-ecosystem standard and bundles touch/swipe, keyboard nav, arrows and a11y that the hand-rolled carousel lacked. ~30 KB gz added to the lazy-loaded route chunk only. Interaction tests are delegated to the library; component tests assert slide rendering + conditional arrows. |
| Mobile nav opens a fullscreen overlay dialog rather than an inline dropdown (user request, 2026-08-22) | Same `<nav>` element restyled below 640px via `.site-nav--open` (fixed inset-0, centred links) so desktop keeps the plain inline row; gains `role="dialog"`/`aria-modal` only while open, body scroll lock, Escape-to-close, hamburger→X morph, focus moves to first link on open and back to the toggle on close. |
| Awards section redesigned (user request, 2026-08-22): one text block + two picture slots, replacing the per-award list | User confirmed the whole section becomes a single editable text chunk plus exactly two image slots; individual award titles disappear. Data lives on `MusicianPageContent` (`awardsText`, `awardsFirstImage`, `awardsSecondImage`) instead of a folder collection — nothing left to order, so no collection is created; `AwardEntry`/`normalizeAward`/`useAwards` removed and seed entries deleted. Missing picture slots render surface fallback blocks; both empty hides the grid entirely. |
| About/Contact headings are CMS-editable fields with hardcoded defaults (`aboutHeading`, `contactHeading`) | Extends the existing "section headings live in page JSON" decision to the remaining two views so every visible page title stays artist-editable; defaults "About Me"/"Contact". E2e nav expectation updated from h1 "About" to "About Me". |
| Contact page renders `note ?? "Messages go directly to the artist's email inbox."` | Plan requires UI copy explaining replies reach the artist's email even before the artist writes their own note; CMS note overrides the default line rather than duplicating it. |
| Social icons are inline SVGs in a local `SocialIcon.vue`, keyed by hostname via `resolveSocialIconKind()` (no icon-library dependency) | User asked for icons instead of text links; the dependency list stays minimal (`vue`, `vue-router`, `swiper`). Outline-style 24×24 currentColor glyphs cover instagram/youtube/x/facebook/tiktok/spotify/spotlight with a generic link-icon fallback so any future platform still renders sensibly. |
| One social-links DOM list inside `<nav>`, repositioned by CSS: absolutely top-right of the header bar ≥640px, static bottom row in the mobile overlay modal | User placement request ("top right in navbar and bottom in mobile modal"); keeping a single source of truth avoids duplicated link lists drifting apart, and the icons automatically inherit the nav's dialog semantics while the menu is open. Auto-margin flex layout pins socials to the modal bottom while the links stay visually centred. |
| Contact gains an optional `phone` field rendered as a `tel:` link (whitespace/hyphens/parens stripped from the href) | User request to add "a space for a phonenumber"; optional so artists without a public number omit it cleanly. Seed uses Ofcom's fictional `+44 7700 900123` range like the `.example` email. |
| **Design pass adopts an "Ink & Amber" theatrical-playbill aesthetic (2026-08-22 deslop)** | User brief: elegant, distinctive, not "AI slop"; draw from cultural aesthetics; dominant colour + sharp accents. The existing chocolate-truffle palette reads as ink/espresso + amber/caramel on cream paper, so the redesign commits to letterpress-editorial presentation: hairline rules, letterspaced caps, framed imagery, grain texture, dark ink footer band. |
| Fonts: Fraunces (display/headings) + Newsreader (body), loaded via Google Fonts in `index.html` | Distinctive variable serif pairing with real editorial character; avoids banned generic families and overused choices. `classic` preset pairing remapped with Georgia/Cambria fallbacks so content still renders if fonts fail; theme tests assert symbolically so no churn. |
| Palette hex values untouched; atmosphere built from `color-mix` derivatives (`--surface-deep`, `--ink-band`, `--accent-wash`, shadows) | The four palette colours are user-supplied decisions recorded earlier; the redesign changes *presentation* (glows, bands, mats, scrims) rather than overriding their choices. |
| Route transitions: motion-v `AnimatePresence mode="wait"` + router `scrollBehavior` smooth-to-top; MotionConfig honours user reduced-motion preference | User asked for smooth library-based view changes ("use a good free library instead" of hand-rolling). motion-v is the MIT Vue port of Motion; CSS-only fallbacks kept for hover micro-interactions where libraries add nothing. |
| PrimeVue pinned to primevue@4.5.5 + @primevue/themes@4.5.4 (last MIT releases); v5 NOT adopted | User flagged that PrimeVue is no longer open source: verified v5 ships PrimeTek's Community/Commercial dual licence ("SEE LICENSE IN LICENSE.md") while 4.5.x remains MIT. npm deprecation warning on @primevue/themes accepted deliberately; revisit only if licence posture or needs change. Used for themed Contact Button + ScrollTop with a custom Aura-derived preset mapped to the site palette. |
| Cross-links point at each other: Actor page shows "Musician →", Musician page shows "← Actor" | User request: drop the "Also a(n)" phrasing and reverse the musician arrow so the pair visually directs back and forth. |
| Contact page gains optional `contactImage` field, rendered as an offset-framed portrait beside the enquiry panel | User asked for a picture on Contact too; reuses the About portrait's offset-frame treatment for visual continuity. Seed reuses portrait.svg per established reuse precedent. |
| Sveltia CMS loaded from the unpkg CDN at `/admin`; no bundling or version pinning | Zero build integration keeps the app bundle clean; Sveltia is a single JS file and always-current releases are fine for a static admin shell. `config.yml` points at `timviney/artist-portfolio@main` (matches git remote). |
| One files-collection entry per JSON document, one folder collection per media list; folder collections use `format: json`, `create: true`, and an explicit `order` number widget | Mirrors the content layer 1:1 so normalizers never meet shapes the CMS cannot produce (and vice versa). Filename stays the slug; `identifier_field` falls back to `alt`/`description` on image-only collections that lack titles. The awards redesign's "no list" nature means it lives in the musician *page* fields, not a collection. |
| Admin config correctness is enforced by unit tests (`src/__tests__/adminConfig.spec.ts`) parsing the YAML with js-yaml | A malformed `config.yml` only fails at runtime inside the CMS UI; 11 assertions pin backend/media paths, every collection/file/field against the TS types, and guard against an awards collection ever sneaking back. |
| Artist login documented as a fine-grained PAT in `docs/CMS_SETUP.md` | Fine-grained token scoped to this repo with Contents: Read-and-write is the least privilege that satisfies Sveltia's GitHub sign-in-with-token flow; doc covers creation, renewal on expiry, local-repo editing, troubleshooting. Task 13's ARTIST_GUIDE will link rather than duplicate it. |
| CloudFront SPA fallback: `custom_error_response` 403/404 → `/index.html` with status 200, error TTL 60s | S3 + OAC returns 403 for missing keys and 404 for unknown paths; serving index.html with 200 lets vue-router resolve deep links client-side. Short TTL limits how long a genuinely missing hashed asset stays cached as HTML. |
| Caching via AWS Managed-CachingOptimized; custom response-headers policy adds HSTS, nosniff, SAMEORIGIN frame options, strict referrer policy - no CSP | The managed policy gives edge caching with gzip/brotli without hand-tuning TTLs. A restrictive CSP risks breaking YouTube/Vimeo embeds, so security headers stop short of CSP for v1 (revisit if needed). |
| Deploy role trusts only `repo:timviney/artist-portfolio:ref:refs/heads/main`; permissions are s3 List/Get/Put/Delete on the bucket plus cloudfront CreateInvalidation on the distribution | Least privilege: deploys happen exclusively from main, PR CI never touches AWS, and the role cannot read other buckets or modify distribution config. |
| Terraform state stays local (`infra/.terraform` + state files gitignored) with a commented migration path to an S3 backend | Single artist, single operator today; remote locking buys nothing yet but the switch is one `backend "s3" {}` block when collaboration arrives. Bucket name includes the AWS account ID for global uniqueness. PriceClass_100 keeps costs minimal (EU/US/N.America edges only). |
| Header backdrop blur lives on `.site-header::before`, never on the header itself; unknown routes render a NotFoundView catch-all; `/admin` bounces to `/admin/` before app mount | `backdrop-filter` (like filter/transform) turns an element into the containing block for fixed descendants - putting it on the sticky header silently trapped the fullscreen mobile nav inside the bar (user-reported). The catch-all route + redirect guard close the "blank screen" failure mode of the SPA fallback serving the shell for paths vue-router doesn't know (`/admin`, typos, stale deep links). |
| Optional CV download via settings: `SiteSettings.cv` (optional trimmed path) + Sveltia `file` widget; seeded with a generated placeholder PDF | User requested a top-right Download CV button with CMS-uploadable file. Verified feasible (media library handles non-image files), so no stop. Single optional path keeps the model minimal. Originally unseeded, but the user then asked for the button to be present by default, so a scripted placeholder `max-rivera-cv.pdf` (valid minimal PDF 1.4, explicitly labelled for replacement via Settings > Site) ships in the media folder - consistent with the hand-made-placeholder precedent and replaced through the same CMS upload flow. |
| Header right side is one `.site-nav__actions` group (CV pill + socials `<ul>`), repositioned by CSS per breakpoint: absolute top-right cluster on desktop, stacked column at the modal bottom on mobile | Extends the "single social-links DOM list" decision to cover the new button: one source of truth can't drift, and both placements stay pure CSS. The pill sits just inside the socials' corner position rather than displacing them. `download` attribute triggers a file save for same-origin PDFs. |
| Real content migrated as-is with three deliberate normalisations | (1) Bare emoji separator lines on About/Musician intro dropped - decorative, clash with the editorial design; sentences kept verbatim and re-addable via CMS. (2) Straight ASCII quotes replace curly quotes in migrated copy (established convention; avoids console-masking gotchas). (3) Contact note overridden to "Enquiries go to Middleweek Newton Talent Management..." because the default "messages go to the artist's inbox" would be false for an agency address. Tagline invented minimal ("Actor and musician.") since the source site states none. |
| Squarespace-native self-hosted videos are out of scope for the embed-only pipeline | 6 of Max's videos (3 actor showreel scenes, all 3 musician highlights incl. Drum Reel and Overcome) are uploaded directly to Squarespace with no YouTube/Vimeo URL discoverable in the page. The v1 architecture deliberately supports platform embeds only (`toEmbedUrl` fails soft). Migration ships the other 11 platform-hosted videos and leaves Highlights empty-but-headed; resolution requires either Max uploading them to YouTube/Vimeo then adding via CMS, or a future decision to support direct video files (would need hosting + component work). Flagged to the user rather than silently dropping or expanding scope. |
| User hand-tuned palettes in commit c610379 are canonical; tests pin their values | Surfaces changed (truffle #FDFBD4→#fefde9, tropical punch →#ffe3e9, lavender →#fcfae1, stormy →#dcedff) plus five secondaries (#c0c0c0/#b89d1b/#1a2b30/#beb959/#79b2ec). Tests now reference the CHOCOLATE_TRUFFLE constant instead of duplicated literals where possible, hex-format test accepts any case, and main.css :root pre-hydration defaults mirror the new default surface so first paint matches. Registry-wide AA test re-verified all 24 pairings on the new values. |
| Five additional presets (2026-08-23) assigned by role, not by user listing order: salt and pepper = primary #B3B3B3 / secondary #D4D4D4 / surface #FFFFFF / ink #2B2B2B (modern); tropical punch = primary #FF8243 / secondary #FCE883 / surface #FFC0CB / ink #069494 (playful); yacht club = primary #245F73 / secondary #BBBDBC / surface #F2F0EF / ink #733E24 (classic); lavender fields = primary #C1BFFF / secondary #BDB96A / surface #FDFBD4 / ink #CF6DFC (editorial); stormy morning = primary #6A89A7 / secondary #88BDF2 / surface #BDDDFC / ink #384959 (modern) | User supplied four unordered hexes per theme and asked which colour fits which part. Rule applied: lightest → surface (page background), darkest → ink (dark bands), most brand-defining hue → primary, supporting highlight → secondary. Notable calls: tropical punch puts pink as background with teal as ink (lagoon dark band; its on-surface/on-ink text resolves to black since teal-on-pink is 2.4:1); yacht club keeps navy as the blazer-brand primary and leather brown as ink (numerically darker than navy - mahogany trim); lavender fields' vivid orchid is the darkest of its hues so it serves as ink ("fields at dusk"). Two user hexes contained letter O typos, read as zeroes: #FFCOCB → #FFC0CB (standard pink), #F2FOEF → #F2F0EF. Font pairings chosen per mood: modern/playful/classic/editorial/modern respectively. All 24 category pairings across all six presets verified ≥ AA 4.5:1 by test. |
| Stage name restaged Max Young → Max Pavlovsky with a deliberately scoped rename (user request + Q&A, 2026-08-23) | Display name changes everywhere it renders (site.json, project credits). Two things stay by user decision: the "Young & Furey" duo titles (historical collaboration billing with Vianne Furey - renaming would misstate a named act) and the @maxyoungacts Instagram URL (external account whose handle cannot be verified from here). Internal filenames/slugs ARE renamed (maxpavlovsky-* media, max-pavlovsky-cv PDF, headshot entry slugs) so the repo stays consistent even though URLs are invisible to visitors. The CV PDF's *contents* still say whatever the artist uploaded - he replaces the file via CMS as before. |
| Inline links use a tiny custom `[label](url)` renderer, not a markdown dependency (2026-08-23) | The user asked for links in captions/titles/descriptions only - no headings, lists or emphasis. A ~30-line escape-first linkifier with an http/https/mailto scheme allow-list covers that without pulling marked/markdown-it (the v1 decision to avoid a markdown parser still holds for everything else). Escaping before linkification means stored content can never inject HTML; unsafe schemes fail soft to literal text. Alt attributes get `stripInlineLinks()` since they must stay plain strings. Sveltia's markdown widget gives the artist a real link-inserting toolbar while still storing a plain string inside the JSON files, keeping the JSON-not-markdown storage decision intact. |
| Home tiles locked to a 3/4 portrait `aspect-ratio` with labels at opposite top corners; header name upright (2026-08-23) | User design pass: tiles previously used a vh-clamped height (~44rem wide × ≤46rem tall) that read square on wide screens - aspect-ratio keeps them portrait at every width with no viewport coupling (mobile single-column inherits it; the old mobile height override was removed). Labels moved bottom→top ("Actor" anchored left, "Musician" right via per-tile modifier classes) required flipping the legibility scrim to darken the frame's top instead of its bottom; arrows stay in the label cluster. The italic the user saw "in the title at the top" was the sticky header's site name (`font-style: italic`), removed alongside a bump 1.35rem → 1.5rem; the large hero name itself was already upright. |

## Progress Log

| Date | Event |
|---|---|
| 2026-08-22 | Plan created. |
| 2026-08-22 | Verified Sveltia CMS auth via official docs (PAT sign-in, no OAuth server needed); resolved auth unknown. |
| 2026-08-22 | Added draft preview mode (editorial workflow + `?preview=<branch>`) after scope discussion; recorded target user (single artist, "Max"). |
| 2026-08-22 | Deferred draft preview mode to post-v1 per user decision; moved to new Future Enhancements section. |
| 2026-08-22 | Decided single private repo for code + content; two-repo split recorded as future migration path. |
| 2026-08-22 | Flipped to single **public** repo per user preference (portfolio visibility); trade-offs recorded. |
| 2026-08-22 | Task 1 complete. Scaffolded Vite + Vue 3 + TS app in repo root: ESLint 10 flat config + Prettier + vue-tsc typecheck; Vitest 4 (jsdom) with router unit tests; Playwright skeleton with 2 passing e2e specs; vue-router 5 with placeholder routes for Home/Gallery/About/Contact; base stylesheet + planned directory structure (`src/components`, `src/composables`, `content/*`, `public/images/uploads`). User approved Node upgrade to 24.19 LTS during install resolution; TS pinned to ^6.0.3 (vue-tsc lacks TS7 support). All verification green: dev server HTTP 200, lint, typecheck, unit tests (2 passed), e2e (2 passed), production build. |
| 2026-08-22 | Task 2 complete. Built content layer in `src/composables/content/`: TS types for settings/theme/pages/artworks, pure normalizers with per-field fallback defaults, and eager `import.meta.glob('/content/**/*.json')` loaders (`useSiteSettings`, `useTheme`, `useHomePage`, `useAboutPage`, `useContactPage`, `useArtworks` sorted by `order`, `useArtwork(slug)`). Seeded fictional artist "Max Rivera": site/theme/home/about/contact JSON + 6 artworks incl. one YouTube video entry; 8 SVG placeholder images in `public/images/uploads/`. 26 new unit tests cover normalizer fallbacks and seeded-content loading through the real glob path. Verification green: lint, typecheck, unit tests (28 passed), production build. Note: content module is tree-shaken out of the current bundle because no view consumes it yet - views wire up in tasks 5–8; glob inlining is verified by the Vitest run which uses the same Vite transform pipeline. |
| 2026-08-22 | Design locked with user (Q&A): actor–musician dual portfolio. Nav = Home/About Me/Contact only; Home = two headshot tiles → `/actor` + `/musician`; Actor page = hero, videos, headshots carousel, portrait gallery; Musician page = hero, intro, awards, highlights, original projects, gallery; cross-links between both. Generic Gallery route + artworks collection removed; lightbox becomes image-only (videos inline); headshot carousel stays inline. Plan restructured: task 2b added, tasks 4–14 rewritten/renumbered, Overview gained a "Site structure" section, 5 new decisions recorded. |
| 2026-08-22 | Task 2b complete (user approved immediate implementation). Content layer rebuilt for the new design: types/defaults/normalizers/loaders for `useHomePage` (headshot slots), `useActorPage`, `useActorVideos`, `useHeadshots`, `useActorGallery`, `useMusicianPage`, `useAwards`, `useHighlights`, `useProjects`, `useMusicianGallery`; shared generic `loadEntries(dir, normalize)` sorts by explicit `order`. Router: `/gallery` removed, `/actor` + `/musician` added with placeholder views; nav placeholder = Home/About Me/Contact; e2e spec updated to match (not run). Content reseeded as an actor-musician persona across 7 new folder collections (14 entry files); placeholder SVGs reused where they fit (portrait → headshots, scenic → heroes/galleries), `artwork-studio-process.svg` deleted, no new images created. 36 content tests + router tests green. Verification: lint ✓, typecheck ✓, unit tests 38 passed, production build ✓ (all five view chunks emitted). |
| 2026-08-22 | Task 3 complete. Theme system implemented as a named-preset registry: `src/composables/theme/` (types, presets, resolver/mapper) - `THEME_PRESETS` currently holds one preset, "chocolate truffle" (#713600 primary, #C05800 accent, #FDFBD4 background, #38240D text, classic font pairing); `content/settings/theme.json` now stores `{ "preset": "chocolate truffle" }` and unknown/missing selections fall back to the default preset. `themeToCssVariables()` maps palette+fonts to the seven app CSS custom properties (border derived as rgba(text, 0.15)) and is bound to the app root in App.vue; `:root` stylesheet defaults mirror the default preset. WCAG contrast utilities added and asserted in tests. Verified: lint ✓, typecheck ✓, unit tests 47 passed (9 new theme tests), production build ✓ with theme confirmed present in the emitted bundle. Future Enhancements gained "front-end theme switcher". |
| 2026-08-22 | Palette model revised per user correction: the four colours are four equal categories (primary / secondary / surface / ink) rather than three roles + fixed text. Text per category auto-derived via `resolveOnColors()` - palette-coherent candidate when it meets AA 4.5:1, otherwise higher-contrast of white/black. Computed result for chocolate truffle: on-primary cream, on-secondary **black** (4.64:1 beats white's 4.53:1 on caramel - initial white assumption corrected by the contrast math), on-surface espresso, on-ink cream. Emits 8 semantic vars (`--color-primary/secondary/surface/ink` + `--color-on-*`) plus legacy `--color-bg/text` aliases so existing CSS keeps working; border = rgba(ink, 0.15). Verified: lint ✓, typecheck ✓, unit tests 50 passed, build ✓. |
| 2026-08-22 | Task 4 complete. Built shared layout: `SiteHeader.vue` (site name from settings linking home; nav = Home/About Me/Contact only; active-route styling via `router-link-exact-active`; hamburger toggle below 640px with `aria-expanded`/`aria-controls`, menu closes on link click and on route change) and `SiteFooter.vue` (social links with `rel="noopener noreferrer"` + © year/name). Both consume `useSiteSettings()` directly. `App.vue` now composes header/main/footer in a flex column shell (sticky footer); `.visually-hidden` utility added globally. Router route table exported as `routes` so tests build isolated memory-history routers (decision logged). 8 new component tests (nav links, active class, toggle, auto-collapse, footer socials/copyright) read real seeded content through the actual glob path - e2e link names unchanged so existing specs stay valid (not run per plan note). Verification: lint ✓, typecheck ✓, unit tests 58 passed (+8), production build ✓. |
| 2026-08-22 | Task 5 complete. `HomeView.vue` rebuilt from placeholder: artist name (`h1`) + tagline from site settings, two side-by-side portrait-ratio headshot tiles linking to `/actor` and `/musician`, each with a label; missing image slots render a styled fallback block instead of an `<img>` (graceful-missing behaviour). 4 component tests in `src/views/__tests__/HomeView.spec.ts` mock `useHomePage` over the real module (settings stay real seeded data) to cover CMS-driven images, partial-missing, and full-missing cases. E2e home spec updated for the new `h1` ("Max Rivera" replaces placeholder "Home"; not run per plan note). Verification: lint ✓, typecheck ✓, unit tests 62 passed (+4), production build ✓ with a dedicated HomeView chunk. |
| 2026-08-22 | Task 6 complete. `ActorView.vue` rebuilt: full-viewport hero (`100svh`, cover image or surface fallback) → "Actor" heading with video articles (title/`VideoEmbed`/description) → "Gallery" heading with Headshots carousel + Gallery Images portrait grid → "Also a musician" cross-link to `/musician`. New reusable pieces: `VideoEmbed.vue` (16:9 iframe wrapper, renders nothing without a resolvable URL) and `toEmbedUrl()` in `src/composables/video.ts` converting YouTube watch/share/shorts/embed + Vimeo links to privacy-enhanced embed URLs (6 unit tests; decision logged). Carousel: translateX track, prev/next buttons disabled at ends, visible counter with `aria-live`, `aria-roledescription="carousel"`, arrow-key support via keydown on the group; controls hidden for single/empty lists; missing slide images get fallback blocks. 7 ActorView component tests mount against real seeded content by default (loaders wrapped via `vi.fn(actual)` + `mockReset()` restores real impls) with overrides for multi-slide navigation and empty-content graceful handling. Note: seed gallery titles use em-dashes ("As Prospero - The Tempest") - PowerShell console output had masked this, first test run caught the mismatch and expectations were corrected. Verification: lint ✓, typecheck ✓, unit tests 75 passed (+13), production build ✓ with dedicated ActorView chunks. |
| 2026-08-22 | Actor view tweak pass per user feedback. Headshots carousel replaced with **Swiper** (`swiper/vue`, Navigation + Keyboard + A11y modules) for real flick/swipe support - dependency addition recorded as a decision; arrows render only with >1 slide. Layout changes: "Actor"/"Gallery" are now large centred titles (`clamp(2.25rem–3.5rem)`); videos span full page width with description centred beneath the embed and top titles removed; headshot carousel centred (42rem cap) directly under the Gallery title acting as its highlight; "Gallery Images" subheading removed from the grid; gallery grid upsized to 1 col → 2 cols ≥640px → max 3 cols ≥1024px. Content layer: unused `headshotsHeading`/`imagesHeading` fields dropped from `ActorPageContent` (type/default/normalizer/seed/tests) so the CMS never exposes dead knobs. Component tests updated (ResizeObserver stubbed for Swiper in jsdom; slide-count + conditional-arrows assertions replace hand-rolled nav tests). Verification: lint ✓, typecheck ✓, unit tests 76 passed, production build ✓ (ActorView chunk lazy-loaded, ~30 KB gz Swiper cost). |
| 2026-08-22 | Width tweak per user feedback ("too much buffer on the sides"): added a `.page--wide` modifier (max-width 90rem vs 72rem default) applied to the Actor videos and gallery sections; headshot Swiper widened 42rem → 60rem. Tests made seed-count agnostic after discovering `content/actor/headshots/main-headshot copy.json`, an exact duplicate of `main-headshot.json` that appeared outside the session - left in place (user content; delete if unintended), single-vs-multi carousel cases now mocked explicitly instead of relying on seed counts. Verification: lint ✓, typecheck ✓, unit tests 77 passed, production build ✓. |
| 2026-08-22 | Unified content width per user request ("make it all the same wide"): global `.page` container now 90rem for every view; the `.page--wide` modifier removed again along with its ActorView usages; Home headshot tiles no longer capped at their own 56rem; header/footer inner containers widened to match (90rem). User confirmed `main-headshot copy.json` is an intentional duplicate added to exercise carousel scrolling - kept, and count-agnostic tests stay valid. Verification: lint ✓, typecheck ✓, unit tests 77 passed, production build ✓. |
| 2026-08-22 | User hand-tuned widths to a preferred 90rem and reworked the header bar (absolutely centred site name, left-aligned nav, right-side toggle) - preserved as-is. Mobile nav rebuilt per user request into a **fullscreen overlay dialog**: below 640px the same `<nav>` becomes fixed inset-0 with centred large links; `role="dialog"` + `aria-modal` bound only while open, body scroll locked while open, Escape closes, hamburger morphs to an X, focus moves to the first link on open and returns to the toggle on close. Four new header tests (dialog semantics, scroll lock, Escape close; existing toggle/route-change/link-click kept green). Two seed-drift test failures surfaced: seeded video URLs now use YouTube ID `L_LUpnjgPso` - expectations aligned in video + ActorView specs. Verification: lint ✓, typecheck ✓, unit tests 80 passed (+4), production build ✓. |
| 2026-08-22 | Responsive tweak: Home headshot tiles collapse from 2 columns to 1 below 640px (matches the app's existing mobile breakpoint). Verification: lint ✓, typecheck ✓, unit tests 80 passed, production build ✓. |
| 2026-08-22 | Home tile labels ("Actor"/"Musician") now overlay their images instead of sitting beneath: absolute bottom-centred heading text in white with text-shadow over a bottom gradient scrim (predictable legibility over any uploaded photo); hover/focus switches to a white underline (primary-colour hover removed as unreadable over photos). Verification: lint ✓, typecheck ✓, unit tests 80 passed, production build ✓. |
| 2026-08-22 | Task 7 complete (implemented in a prior working session, committed by user as `2983ceb lightbox`, verified this session). `ImageLightbox.vue`: teleported fullscreen overlay dialog (`role="dialog"`, `aria-modal`), prev/next with wrap-around, caption + "n / total" counter (`aria-live`), arrow-key navigation, Escape and backdrop-click close, close button, body scroll lock, focus moved in on mount and restored on unmount, image-only (videos stay inline per decision), fallback block for missing images. Wired into ActorView's gallery grid (buttons with `cursor: zoom-in` open it; entries without images render fallbacks and no trigger). 11 component tests. Verification: lint ✓, typecheck ✓, unit tests 94 passed, production build ✓. |
| 2026-08-22 | Task 8 complete. `MusicianView.vue` rebuilt from placeholder, mirroring ActorView patterns: fullscreen `100svh` hero (cover image or surface fallback) → "Musician" heading + centred intro → Awards (title + text per entry, optional image above) → Highlights and Original Projects (`VideoEmbed` articles with centred descriptions, no top titles) → Gallery (portrait grid, description below each image, images open the shared `ImageLightbox` with descriptions as captions) → "Actor →" cross-link. All five section headings always render from page JSON; only lists are conditional on content (matches ActorView's graceful-missing behaviour). 13 component tests mount against real seeded content with loader mocks over the real module (hero/headings/intro/awards ± image, highlights, projects, gallery + lightbox open/close, no-trigger-without-image, actor cross-link, empty-sections graceful case). Recurring gotcha re-confirmed: PowerShell console masks em-dashes - seeded award title "OffCommendation - Best Supporting Performance" uses a real U+2014; byte-level check settled it and the expectation was corrected. Verification: lint ✓, typecheck ✓, unit tests 107 passed (+13), production build ✓ with a dedicated lazy-loaded MusicianView chunk. |
| 2026-08-22 | Awards section redesigned per user request ("one chunk of text and two pictures"; whole-section interpretation confirmed via Q&A - the per-award list is gone entirely). Content model: `AwardEntry`/`normalizeAward`/`useAwards` and the `content/musician/awards/` collection deleted; `MusicianPageContent` gained `awardsText` + `awardsFirstImage`/`awardsSecondImage` (optional, trimmed). Seed: both former award texts merged into one paragraph on `pages/musician.json`; picture slots reuse the matched pair `artwork-tide-lines-i/ii.svg` (all placeholders already in use; reuse precedent exists via duplicated portrait.svg), awards folder removed. View: centred text block + two side-by-side 3/4 picture slots with surface fallbacks per missing slot; grid hidden when both empty. Tests: loader spec lost the two award tests and gained normalizer + seeded-slot coverage; MusicianView spec now 14 tests (text+pictures from CMS, single-slot fallback, fully-empty hide, plus prior coverage). Also aligned seed text to the user's ASCII-hyphen convention (commit cd8ce85 had normalized the old em-dash seeds). Verification: lint ✓, typecheck ✓, unit tests 108 passed, production build ✓. |
| 2026-08-22 | Task 9 complete. About/Contact views rebuilt from placeholder. Content model: `AboutPageContent` gained `aboutHeading`, `ContactPageContent` gained `contactHeading` (defaults "About Me"/"Contact", CMS-editable per the headings-in-page-JSON decision); seeds updated. **About**: large centred title, portrait (3/4, bordered, fallback block when missing) beside the bio paragraphs ≥640px / stacked below, optional statement rendered as a centred italic serif quote block with a top border on desktop. **Contact**: centred title, pill-shaped primary-colour "Enquire by email" mailto button (secondary-colour hover using on-secondary text), the email address repeated as a mailto link below, social links list (target=_blank + rel=noopener noreferrer, hidden when none), note copy = CMS `note ?? "Messages go directly to the artist's email inbox."`. E2e nav expectation updated h1 "About" → "About Me" (not run per plan note). 10 new component tests (5 per view: seeded rendering, statement/note presence + fallbacks, missing-portrait fallback, safe social links, empty-socials case). Verification: lint ✓, typecheck ✓, unit tests 118 passed (+10), production build ✓ with dedicated lazy-loaded About/Contact chunks. All five routes now have real views; only tasks 10-14 remain. |
| 2026-08-22 | Header socials + contact phone per user request ("add a space for a phonenumber... 3 social links to the navbar and modal... use icons... top right in navbar and bottom in mobile modal"). New `SocialIcon.vue` (inline outline SVGs, currentColor) + `resolveSocialIconKind()` hostname resolver (instagram/youtube/x/twitter/facebook/tiktok/spotify/spotlight → glyphs, unknown → generic link icon; 3 unit tests). `SiteHeader.vue`: nav links wrapped in `.site-nav__links`, seeded socials (Instagram/YouTube/Spotlight) rendered once inside `<nav>` as icon links (aria-label, target=_blank, rel=noopener noreferrer) - absolutely positioned top-right of the header bar on desktop, static bottom row inside the fullscreen modal via auto-margin flex (links stay centred, icons enlarge to 1.75rem). Contact: optional `phone` field (type/default/normalizer/seed `+44 7700 900123` Ofcom fictional) rendered as a `tel:` link with separators stripped from the href; hidden when unset. Tests +4 header (icon links, modal placement), +2 contact (tel link, absent-phone case), +1 loader (seeded phone). Verification: lint ✓, typecheck ✓, unit tests 125 passed (+7), production build ✓. |
| 2026-08-22 | Design pass ("deslop") complete. Behaviour: sticky translucent blurred header (scrolled elevation state), router scrollBehavior smooth-scrolls to top on every navigation (jsdom scrollTo stub added to new vitest setup file). Typography: Fraunces + Newsreader via Google Fonts, classic pairing remapped. Atmosphere: paper-grain/warm-glow body overlay, color-mix derived tokens, accent-washed gallery bands, ink-band footer with icon socials. Views restyled keeping all test-relevant class names: Home playbill lockup with motion-v staggered entrance + framed zoom tiles; Actor/Musician hero title overlays (section-title counts preserved) with letterspaced captions, ruled video separators, framed headshot swiper (mat recoloured to --surface-deep after user flagged near-white gap); About offset-frame portrait + pull-quote statement; Contact rebuilt around PrimeVue Button (as=a mailto), tel/email links, icon socials, and NEW optional contactImage field (seeded portrait.svg, offset frame). Motion: motion-v AnimatePresence mode=wait route transitions under MotionConfig reduced-motion=user; CSS micro-interactions; staggered mobile-modal links. Cross-links simplified per user: "Musician ->" / "<- Actor". PrimeVue pinned to MIT primevue@4.5.5 + @primevue/themes@4.5.4 after verifying v5 ships PrimeTek commercial dual licence (user catch); custom Ink & Amber Aura preset shared via composables/theme/primePreset.ts; ScrollTop in App shell. Tests updated: footer icons assert aria-labels, ContactView mounts with PrimeVue plugin (+2 image tests), Home label assertions tolerate arrow glyph. Verification: lint OK, typecheck OK, unit tests 127 passed, production build OK (entry bundle ~124 KB gz from motion-v + PrimeVue config - noted, acceptable for v1). Plan gained task 9b (complete), 7 decisions, stray [x] on task 10 first subtask reverted (public/admin does not exist). |
| 2026-08-22 | Task 10 complete (one interactive subtask deferred). Added public/admin/index.html loading Sveltia CMS from unpkg CDN and public/admin/config.yml mirroring the content layer exactly: Settings files-collection (site.json name/tagline/socialLinks list + theme.json select with chocolate truffle option), Pages files-collection (home/about/contact/actor/musician JSON incl. contactImage + phone + awards text/photo fields), and six folder collections (actor videos/headshots/gallery, musician highlights/projects/gallery) using format json, create true, identifier_field fallbacks (alt/description on image-only lists) and required order number widgets. Media folder public/images/uploads with /images/uploads public path; backend github timviney/artist-portfolio@main per git remote. Correctness enforced by 11 js-yaml unit tests parsing the YAML against the TS types incl. a guard that no awards collection returns; devDeps added js-yaml, @types/js-yaml, @types/node (node types referenced via triple-slash in the spec only). Serving verified: vite preview returned HTTP 200 for /admin/ (sveltia script present) and /admin/config.yml; Playwright smoke spec written at tests/e2e/admin.spec.ts but not run per standing plan note. PAT setup documented in docs/CMS_SETUP.md (fine-grained token, Contents RW, renewal, local-repo editing, troubleshooting); interactive editor UI walkthrough left as an open subtask needing a human browser session. Verification: lint OK, typecheck OK, unit tests 138 passed (+11), production build OK. |
| 2026-08-22 | Task 11 complete (apply deferred to user). Built infra/ with Terraform ~> 6.0: versioned private S3 bucket (name includes AWS account ID, all public-access blocks on, BucketOwnerEnforced, AES256 SSE), CloudFront distribution with Origin Access Control (sigv4 always), Managed-CachingOptimized cache policy + custom security response-headers policy (HSTS 2y, nosniff, SAMEORIGIN, strict-origin-when-cross-origin; deliberately no CSP to keep YouTube/Vimeo embeds working), SPA fallback via custom_error_response 403/404 -> /index.html @200 with 60s error TTL, bucket policy restricted to the distribution by AWS:SourceArn. IAM: GitHub OIDC provider + deploy role trusted only for timviney/artist-portfolio@refs/heads/main with s3 List/Get/Put/Delete and cloudfront CreateInvalidation only. Outputs expose bucket name, distribution id/domain/url and deploy role ARN (for task 12). Validation ran against real credentials: terraform fmt clean, terraform validate OK (caught two provider-v6 syntax changes: custom_error_response singular block name, apply_server_side_encryption_by_default rename - fixed), terraform plan = 12 to add / 0 change / 0 destroy. Apply NOT run per plan note - user executes terraform apply when ready. .gitignore gained Terraform state/cache entries; three Current Unknowns resolved (SPA routing behaviour designed in, visual direction settled by the design pass, editorial-workflow question moot while draft preview stays post-v1). |
| 2026-08-22 | Two user-reported bugs fixed. (1) Mobile menu no longer fullscreen: the design pass had put backdrop-filter blur on the sticky .site-header, which made it the containing block for the fixed-position fullscreen nav and trapped the overlay inside the header bar; blur moved to a ::before pseudo-element (identical visual, no containing-block side effect) with an explanatory comment. (2) /admin showed nothing: without a trailing slash the SPA fallback served the app shell for a route vue-router did not know, rendering an empty main - confirmed by preview probes (/admin returned the 934-byte app shell vs /admin/ serving the 541-byte CMS page). Fixes: main.ts now bounces pathname /admin to /admin/ via location.replace before mounting, and a NotFoundView catch-all route (/:pathMatch(.*)*) renders a styled 404 page instead of a silent blank for any unknown path (+1 router test). CMS_SETUP.md gained a How-it-works intro and blank-page/script-blocking troubleshooting entries. Verified: lint OK, typecheck OK, 139 tests passed, build OK, /admin/ serves CMS page with sveltia script present. |
| 2026-08-22 | Two user-reported changes. (1) Gallery tint bands were full-bleed no more: the accent-wash gradient sat on the .page container, so above 90rem it stopped at the margins leaving hard vertical seams - gradient moved to a new global .band wrapper that spans the viewport edge to edge with the .page content inside (Actor + Musician galleries). (2) Manual ordering replaced: every entry collection swapped order:number for dateAdded (optional ISO datetime) across types/normalizers/loaders; loadEntries now sorts newest-first via localeCompare with slug tie-break and undated entries last. Seeds migrated preserving exact current display order - which surfaced content drift: user had added content/musician/gallery/making-sounds.json outside the session (like main-headshot copy earlier); migrated too and tests updated to expect two gallery entries newest-first (field-notes-artwork > making-sounds). Sveltia config: six number widgets became datetime widgets labelled "Date added" (prefills now on creation), summaries dropped #{{order}}, sortable_fields lead with dateAdded; CMS_SETUP.md ordering paragraph rewritten. Verification: lint OK, typecheck OK, unit tests 139 passed, production build OK. |
| 2026-08-22 | Gallery tint made actually visible (user follow-up: "tint does not show at all"). Root cause found by probing the live page with Playwright + pixel-sampling the screenshot: --accent-wash was color-mix(transparent, secondary 12%) = amber at 12% alpha, only ~6 RGB units from the cream background - imperceptible on its own; users had previously noticed the band solely because the margin seams gave it an edge. Token redefined as an opaque tint (surface mixed 16% toward secondary); rendered band centre now measures R246 G224 B181 vs background R253 G251 B212 with soft transparent fades at both ends - verified by decoding band.png pixels via System.Drawing. Verification: lint OK, typecheck OK, 139 tests passed. |
| 2026-08-22 | Headshot frame mat clash fixed (user follow-up to the tint pass): .headshot-swiper dropped its fixed --surface-deep fill in favour of a transparent mat - no static colour can track a full-bleed gradient at every fade position, so the band simply shows through the frame padding. Pixel-probed via clipped screenshots: mat vs adjacent band now within 1-2 RGB units (anti-aliasing noise) at multiple heights; hairline border kept for definition. |
| 2026-08-22 | UI polish per user feedback: headshot Swiper frame border removed (transparent borderless mat on the gradient band), and the PrimeVue ScrollTop jump-to-top button removed from the app shell (smooth scroll-to-top on navigation remains). PrimeVue stays in use for the Contact enquiry Button. Verification: lint OK, typecheck OK, 139 tests passed, production build OK. |
| 2026-08-23 | Task 11b complete (scope addition before task 12, per user: "let's add some more theme presets"). Five new entries in `THEME_PRESETS` from user-supplied unordered colour lists, categories assigned by rule (lightest→surface, darkest→ink, signature hue→primary, highlight→secondary): salt and pepper (modern), tropical punch (playful), yacht club (classic), lavender fields (editorial), stormy morning (modern). Two hex typos corrected (#FFCOCB→#FFC0CB, #F2FOEF→#F2F0EF). Sveltia theme select now lists all six presets; adminConfig spec asserts exact registry↔options parity instead of a single `toContain`. Theme spec gained per-preset palette/fontPairing pins, a registry-wide AA on-colour test (all 24 pairings ≥ 4.5:1) and a six-digit-hex format guard. Seed `theme.json` left on the user's own "yacht club" selection; `:root` CSS defaults untouched. Verification: lint ✓, typecheck ✓, unit tests 146 passed (+7), production build ✓. |
| 2026-08-23 | Theme-application bug fixed after user report ("lots of the colours do not change when you change the theme! The background and the footer ... seem to be stuck at chocolate truffle") - surfaced by the new presets. Root cause was dual: (1) theme vars were bound as inline style on the `.app-shell` div inside `#app`, so `body { background-color: var(--color-bg) }` and every ancestor-level consumer only ever saw the stylesheet `:root` defaults; (2) derived atmosphere tokens (`--surface-veil/deep`, `--ink-band`, `--accent-wash`, `--shadow-lift`) are declared at `:root`, where var() references substitute at declaration time - baking in the default palette for footer bands, gallery washes, mats and shadows everywhere. Fix: new `applyThemeToDocument()` sets all theme variables inline on `document.documentElement` before mount in main.ts; App.vue shell binding removed (decision row revised). Same pass completed the deferred PrimeVue fix: `.contact-button` now overrides the ten `--p-button-primary-*` tokens with site variables so the plugin's once-generated static Ink & Amber palette no longer leaks (user asked "it doesn't change with the theme?? why not"). Verified live via preview-server probes under the seeded yacht club preset: body bg rgb(242,240,239) = #F2F0EF exactly; footer band derives from #733E24; Contact button rgb(36,95,115) navy with off-white label. Seeded-theme loader test made preset-agnostic (registry membership) since the artist flips presets via CMS. Verification: lint ✓, typecheck ✓, unit tests 147 passed, production build ✓ + live probes. |
| 2026-08-23 | /admin redirect guard re-enabled per user request ("oh pls quickly reenable that admin route"). The previously commented-out guard in main.ts is active again - and fixed a latent loop bug while doing so: the committed snippet redirected `/admin` → `/admin` (same URL, infinite replace); it now redirects to `/admin/` with the trailing slash. Probe on the built site: `/admin` lands on `/admin/` with the Sveltia script present and no app shell mounted. Verification: lint ✓, typecheck ✓, 147 unit tests ✓, build ✓ + live probe. Next: task 12 (GitHub Actions workflows). |
| 2026-08-23 | Task 11c complete: header "Download CV" button backed by a CMS-uploadable file (user request with stop-if-impossible condition; Sveltia media library + `file` widget verified capable, so no stop). Content model: optional `cv` on SiteSettings (type/normalizer, trimmed; unseeded so the button stays hidden until the artist uploads). Sveltia site.json gained required:false "CV (PDF download shown in the header)" file widget; adminConfig spec pins field order + widget. SiteHeader: socials and the new CV pill now live in one `.site-nav__actions` group inside the nav - desktop absolute top-right cluster (pill just inside the socials' corner spot), mobile fullscreen modal stacks the pill above the icon row; link carries the `download` attribute. Also absorbed user content drift from commit c610379 ("themes"): four preset surfaces + five secondaries hand-tuned - test pins updated to the user's values, CHOCOLATE_TRUFFLE constant now reused instead of duplicated literals, hex-format test case-insensitive, main.css :root defaults re-mirrored to #fefde9 family; registry-wide AA still green across all 24 pairings. Verified live via preview probe with a temporary seed cv (reverted after): desktop pill at top≈10px/right of socials by 20px with download attr; present in the opened mobile modal. Verification: lint ✓, typecheck ✓, unit tests 150 passed (+3), production build ✓. |
| 2026-08-23 | Default CV seeded per user request ("create a fake pdf and add it so that the cv is there by default"; mobile-nav presence confirmed as already working from the actions-group design). Generated `public/images/uploads/max-rivera-cv.pdf` - a valid 1,613-byte minimal PDF 1.4 built by a throwaway Node script (correct xref byte offsets, Helvetica text) whose visible content says it is a placeholder and gives the CMS replacement path; committed to the media folder like the SVG placeholders before it. `content/settings/site.json` now sets `"cv": "/images/uploads/max-rivera-cv.pdf"`, so the header button renders out of the box. SiteHeader specs updated: the hide case mocks empty settings explicitly, a new seeded case pins the default href/download attr. Probe on the built site: desktop button top-right with download attr, present in the opened mobile modal with the same href; PDF serves HTTP 200 application/pdf starting `%PDF-`. Verification: lint ✓, typecheck ✓, unit tests 150 passed, production build ✓ + live probes. |
| 2026-08-23 | Task 11d complete: full content migration from the artist's real site maxyoungacts.com (user request - "Replace the content with the data"). Scraped all five pages (markdown for copy, raw HTML for embeds since Squarespace wraps YouTube in embedly iframes and stores Vimeo titles in data-html attributes); resolved every video via oEmbed lookups. Downloaded 40 assets (~30 MB) into the media folder at CDN ?format=2500w quality: real `max-young-cv-2026.pdf` from /s/ (replacing the generated fake), 7 Yellowbelly headshots, actor/musician heroes, home tiles, 17 actor gallery images (Eternal Hourglass x4, Henry V x2, Sky in the Pie x3, Hamlet, The Dip x3, Haymarket x3, Dreamland), 2 awards photos, about portrait, Hospital Lights contact still, 8 musician gallery images. Content rewritten faithfully with ASCII-normalised quotes and dateAdded timestamps preserving exact source display order everywhere. Site is now Max Young: agency contact block (agents@mntalent.co.uk, 02033940079, agency note), Who Am I? about bio, Eat The Rich awards text, 2 actor videos (The Eulogy, Hospital Lights MV), 9 musician projects incl. Young & Furey 1/2 + Into the Blue Vimeo collabs. **FLAG (user-facing)**: 6 videos are Squarespace-native self-hosted files with no platform URL and were NOT migratable under the embed-only architecture - actor Ken's Red speech/Kin/Henry V scene, and ALL three musician Highlights (Drum Reel, Overcome, Overcome live), leaving that section empty-but-headed; needs YouTube/Vimeo uploads via CMS or a future direct-file support decision. Superseded placeholder SVGs + fake CV deleted; 36 seed-pinned test expectations realigned to the new seed; e2e home/about expectations updated (not run per standing note). Verified: lint ✓, typecheck ✓, unit tests 150 passed, production build ✓, live preview probe (home h1 Max Young + 2 tiles, actor 2 embeds/7 slides/17 items/hero pixel-loaded, musician 9 project embeds/8 gallery items, CV HTTP 200 application/pdf). |
| 2026-08-23 | Task 11e complete: stage name restaged from Max Young to Max Pavlovsky (user request before task 12; scope confirmed via Q&A - duo billing "Young & Furey" KEPT as historical collab name, Instagram handle @maxyoungacts KEPT as unverifiable external account, internal filenames renamed). git mv'd 11 media files (maxyoung-* -> maxpavlovsky-*, max-young-cv-2026.pdf -> max-pavlovsky-cv-2026.pdf) + 7 headshot entry slugs in content/actor/headshots/; textual replace of paths/name across content JSONs (site.json "Max Pavlovsky", 3 project descriptions now credit "original music by Max Pavlovsky", page image references) and 8 test files incl. e2e home.spec.ts. Verified: lint ✓, typecheck ✓, unit tests 150 passed, production build ✓. Next: task 11f (hardcoded copy -> CMS fields). |
| 2026-08-23 | Task 11f complete: last hardcoded copy moved into CMS-editable fields. Audit found five user-facing hardcoded strings; nav links, Home tile labels and the 404 page stay structural per the fixed-layout decision (aria-labels/a11y strings also stay in code). New required-with-default fields following the headings pattern: `ActorPageContent.heroCaption` ("Stage & screen"), `MusicianPageContent.heroCaption` ("Cello · Guitar · Song"), `AboutPageContent.aboutEyebrow` ("The person behind the work"), `ContactPageContent.contactEyebrow` ("Bookings & enquiries") + `enquiryButtonLabel` ("Enquire by email") - types/defaults/normalizers/seeds/config.yml widgets all wired; views now bind them. Tests: adminConfig field lists updated, loader specs pin new defaults + override behaviour (+1 test), view specs' mock literals extended (typecheck caught all 11 stale mocks) and seeded render assertions added for caption/eyebrow/button text. Verified: lint ✓, typecheck ✓, unit tests 151 passed, production build ✓. Next: task 11g (inline links). |
| 2026-08-23 | Task 11g complete: inline links in captions/titles/descriptions so Max can credit photographers. New dependency-free `src/composables/richText.ts`: HTML-escape first, then linkify only `[label](url)` with an http/https/mailto allow-list (javascript:/data:/etc. fail soft to literal text), external links get target=_blank + rel=noopener noreferrer, mailto stays in-tab; `stripInlineLinks()` reduces links to labels for plain-text contexts. `RichText.vue` renders the escaped HTML (v-if on empty, underline styling inheriting currentColor). Applied to: actor-video descriptions, musician highlight/project descriptions, actor gallery titles, musician gallery descriptions, and lightbox captions; img alt attributes (grid + lightbox) use stripped plain text; video titles untouched (they only feed iframe aria-titles). Sveltia: those five fields switched from string/text to the markdown widget (artist gets a link toolbar) with a `[Photographer](url)` hint; adminConfig gained a test pinning exactly which fields are markdown-enabled. Tests: 8 renderer unit tests incl. XSS cases (one initial expectation bug fixed - escaping runs before link detection, so rejected-link fallbacks contain entities); lightbox caption-link test asserting rich caption + plain alt; view tests for credit anchors in actor titles and musician descriptions. Verified: lint ✓, typecheck ✓, unit tests 163 passed (+12), production build ✓. Artist-facing usage docs land with task 13's ARTIST_GUIDE. Next: task 12 (GitHub Actions workflows). |
| 2026-08-23 | Task 11h complete: home-screen design tweaks per user request before task 12. (1) Tiles now portrait at every width: the vh-clamped frame height (~square at ~44rem tile width on wide screens) replaced by `aspect-ratio: 3/4` - probe shows 686×915 frames at a 1600px viewport; mobile single-column inherits the ratio so its old height override was deleted. (2) Labels moved to opposite top corners ("Actor" top-left, "Musician" top-right) via new `home__tile--actor/--musician` modifier classes with corner anchoring; the bottom legibility scrim flipped to darken the top of each tile. (3) The italic "title in the top" was identified as the sticky header's site name - `font-style: italic` removed from `.site-header__name` and size bumped 1.35rem → 1.5rem (the hero name was already upright). HomeView spec pins the modifier classes. Verified: lint ✓, typecheck ✓, unit tests 163 passed, production build ✓, live preview probe (computed aspect-ratio 3/4, label offsets 27px from top / 32px from outer edges both sides, header name font-style normal @24px Fraunces) + screenshot review. Next: task 12 (GitHub Actions workflows). |
