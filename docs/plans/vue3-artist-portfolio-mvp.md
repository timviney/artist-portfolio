# Artist Portfolio Website - Vue 3 MVP

## Overview

Build a customisable, single-artist portfolio website for an **actor-musician** using Vue 3 + TypeScript, hosted statically on AWS (S3 + CloudFront), with automated deployment via GitHub Actions.

The purpose (from README): allow artists to host their own portfolio website without paying for Squarespace-style services. A technically-minded person sets up the project once (deploy infra, connect GitHub); after that, the artist customises everything themselves through a browser-based editor with **no development skills and no AWS backend**.

Customisation works via **Sveltia CMS**, a Git-based CMS served at `/admin`: the artist edits content (bio, contact info, headshots, photos, videos, theme colours/fonts) and uploads images in a web UI; changes commit directly to the GitHub repo and the CI pipeline rebuilds and redeploys the site (~1–2 min publish delay). Videos are supported by embedding YouTube/Vimeo URLs - nothing is self-hosted. A draft preview mode was designed but is **deferred to post-v1** (see Future Enhancements).

**In scope (v1):** Home with Actor/Musician headshot tiles, Actor page (hero, showreel videos, headshots carousel, portrait gallery), Musician page (hero, intro, awards, highlight/project videos, gallery), About Me, Contact (links + mailto, no form backend), theme customisation (colours/fonts via CMS), Terraform infrastructure, GitHub Actions CI/CD, documentation for both the technical setter-upper and the non-technical artist.

**Out of scope (v1):** multi-artist tenancy, contact-form backend, custom domain/Route53/ACM, blog, e-commerce, SSR/SEO-heavy rendering optimisations, draft preview mode (deferred - see Future Enhancements), and - critically - **any form of layout composition**: artists edit content and theme only; page structure is fixed in code. No section/page builders (that is Squarespace's product, not this project's). Future flexibility comes from config toggles and layout presets on fixed slots, never from free-form composition.

**agent notes** do not run playwright/e2e tests unless requested as they are slow. Always run the minimum tests/build required. **Never create SVG/placeholder image files unless the user explicitly asks.**

### Site structure (final design, locked 2026-08-22)

The artist works as both actor and musician; the site splits into two halves reached from Home.

- **Nav (all pages):** Home · About Me · Contact - Actor and Musician are deliberately **not** nav items.
- **Home (`/`):** artist name/tagline (from site settings) and two headshots side by side linking to `/actor` and `/musician`.
- **Actor (`/actor`):** fullscreen hero image → "Actor" heading with embedded videos → "Gallery" heading containing Headshots (sideways carousel) and Gallery Images (portrait grid, titles below) → link to Musician.
- **Musician (`/musician`):** fullscreen hero image → "Musician" heading + intro text → Awards (title, text, image) → Highlights (videos with descriptions) → Original Projects (videos) → Gallery (pictures with descriptions) → link back to Actor.
- **About Me (`/about`)** and **Contact (`/contact`)** as previously planned.
- Page skeletons are hardcoded in Vue; **all images and text come from CMS content**, with graceful fallbacks when fields are missing.

## Future Enhancements (post-v1)

Ordered roughly by expected value:

1. **Draft preview mode** - enable Sveltia's editorial workflow (saves become draft commits/PRs instead of direct publishes) and add a `?preview=<branch>` override in the content loaders that fetches draft files from `raw.githubusercontent.com`, falling back to bundled content. Gives the artist full-fidelity preview of unpublished text/images/colours on the real site with zero extra AWS infrastructure. Verify at build time: editorial-workflow + PAT compatibility and raw.githubusercontent CORS/rate limits.
2. **Front-end theme switcher** - visitor-facing theme picker cycling through `THEME_PRESETS` (persisted to localStorage); registry and CSS-variable plumbing already in place from task 3.
3. **Layout toggles & presets** - config-driven section visibility ("show commissions section") and 2–3 gallery layout options. Config knobs on fixed slots; never free-form composition.
4. **Contact form backend** - API Gateway + Lambda + SES replacing the mailto link.
5. **Custom domain** - Route53 hosted zone + ACM certificate added to the existing Terraform.
6. **Multi-artist tenancy** - only if demand appears; current design deliberately excludes it. If this becomes a maintained template distributed to several artists, split content into a private per-artist repo at that point (move `content/` + uploads, add cross-repo checkout and a content-commit→deploy trigger to the workflow).

## Architecture Summary

- **App:** Vue 3 + TypeScript + Vite SPA. Dependencies kept minimal: `vue`, `vue-router`, plus `swiper` for image carousels. State via composables (no Pinia needed at this scale).
- **Content:** JSON files under `content/` (settings/, pages/, actor/, musician/), edited by Sveltia CMS, imported at **build time** via `import.meta.glob`. Fully static output.
- **Images:** uploaded via CMS to `public/images/uploads/`, committed to repo, served through CloudFront.
- **Videos:** embedded YouTube/Vimeo URLs, rendered as iframes inline in the Actor/Musician sections. No video is self-hosted and the lightbox handles images only.
- **Admin:** Sveltia CMS at `public/admin/` (index.html + config.yml matching the content schema). Auth via GitHub personal access token entered once by the artist.
- **Infra:** private S3 bucket + CloudFront (Origin Access Control) defined in `infra/` with Terraform. Default `*.cloudfront.net` certificate. Deploy role via IAM OIDC trust with GitHub Actions.
- **CI/CD:** `.github/workflows/` - CI on PRs (lint, typecheck, tests), deploy on push to `main` (build, sync to S3, invalidate CloudFront).

### Proposed repository layout

```
src/                  # Vue app (components/, views/, composables/, router/, styles/)
content/              # CMS-managed content (settings/, pages/, actor/, musician/)
public/images/uploads/# CMS-uploaded media
public/admin/         # Sveltia CMS entry point + config.yml
infra/                # Terraform configuration
.github/workflows/    # CI + deploy pipelines
tests/e2e/            # Playwright specs
docs/                 # ARTIST_GUIDE.md etc.
```

## Tasks

- [x] 1. Scaffold the Vue 3 + TypeScript project
  - [x] Create Vite + Vue 3 + TS app in repo root (respect existing README/.gitignore/LICENSE)
  - [x] Add ESLint (flat config, Vue + TS plugins), Prettier, `vue-tsc` typecheck script
  - [x] Add Vitest + @vue/test-utils, wire `npm run test`; add Playwright skeleton with one trivial passing spec
  - [x] Set up directory structure (components/views/composables/router/content), base global stylesheet, vue-router with placeholder routes for all four pages
  - [x] Verify: dev server runs, `npm run lint`, `npm run typecheck`, `npm run test`, `npm run build` all pass
- [x] 2. Build the content layer
  - [x] Define TS types + loader composables for: site settings (name, tagline, social links, theme), home content, about page, contact page, artwork entries
  - [x] Load `content/**` at build time with `import.meta.glob`; provide sensible fallback defaults when files/fields are missing
  - [x] Seed `content/` with realistic example data (incl. one video artwork entry) and placeholder images
  - [x] Unit tests for loaders incl. missing-content fallback behaviour
- [x] 2b. Restructure content layer for the actor–musician design (2026-08-22 redesign)
  - [x] Replace the artworks model with actor/musician types + defaults + normalizers (home headshots, actor page + videos/headshots/gallery, musician page + awards/highlights/projects/gallery)
  - [x] Replace loaders: `useHomePage`, `useActorPage`, `useActorVideos`, `useHeadshots`, `useActorGallery`, `useMusicianPage`, `useAwards`, `useHighlights`, `useProjects`, `useMusicianGallery`
  - [x] Routes: drop `/gallery`; add `/actor` + `/musician` with placeholder views; nav placeholder shows Home/About Me/Contact; update router tests
  - [x] Reseed content with an actor–musician persona: new folder collections, reuse kept placeholder SVGs in fitting slots, delete obsolete ones (create no new images)
  - [x] Rewrite loader unit tests incl. fallback behaviour; verify lint/typecheck/test/build
- [x] 3. Implement the theme system
  - [x] Map theme config to CSS custom properties applied at app root (`--color-*`, `--font-heading/body`), driven by a named-preset registry (`THEME_PRESETS` in `src/composables/theme/`)
  - [x] Verify contrast/responsive behaviour across presets; unit test the theme-to-CSS-variable mapping - contrast verified with WCAG ratio unit tests (AA: text/primary ≥ 4.5, accent ≥ 3 vs background); responsive behaviour is preset-independent since themes emit only custom properties (one preset exists today: "chocolate truffle")
  - [x] Revise palette model per user correction: four equal colour categories (primary / secondary / surface / ink) instead of three roles + fixed text; text is auto-derived per category (AA-gated, see decisions)
- [x] 4. Build shared layout and navigation
  - [x] Header with site name + nav links (Home, About Me, Contact only), footer with social links + copyright; active-route styling; responsive mobile menu
  - [x] Component tests for nav rendering and mobile toggle
- [x] 5. Build the Home view
  - [x] Artist name + tagline from site settings; two headshot tiles side by side linking to /actor and /musician
  - [x] Component test: renders CMS-driven headshots, handles missing images gracefully
- [x] 6. Build the Actor view
  - [x] Fullscreen hero image; "Actor" heading with video embeds
  - [x] "Gallery" heading: headshots carousel (sideways prev/next, keyboard accessible) + portrait gallery grid with titles below
  - [x] Cross-link tile/section to the Musician page
  - [x] Component tests: sections render from content; graceful handling of missing images/videos
- [x] 7. Build the lightbox viewer
  - [x] Lightweight custom lightbox (no external modal library): fullscreen overlay for gallery images with prev/next and captions
  - [x] Keyboard navigation (arrows, Esc), click-outside close, body scroll lock; image-only (videos are inline embeds)
  - [x] Component tests: open/close, navigation, caption rendering
- [x] 8. Build the Musician view
  - [x] Fullscreen hero image; "Musician" heading + intro text
  - [x] Awards section as **one text block + two picture slots** (redesigned per user request, 2026-08-22 — replaced the per-award list); Highlights (videos with descriptions); Original Projects (videos); Gallery (pictures with descriptions)
  - [x] Cross-link back to the Actor page
  - [x] Component tests for all sections
- [x] 9. Build About Me and Contact views
  - [x] About: CMS heading, portrait photo, bio paragraphs, optional statement (quote block)
  - [x] Contact: CMS heading, email address, optional phone (`tel:` link), `mailto:` enquiry button, social links; note in UI copy that replies go to the artist's email (CMS note overrides a hardcoded default line)
  - [x] Component tests for both views
- [ ] 10. Integrate Sveltia CMS
  - [x] Add `public/admin/index.html` + `config.yml` whose collections/schema exactly match the content layer types (settings, pages, actor videos/headshots/gallery, musician highlights/projects/gallery; media folder `public/images/uploads`)
  - [ ] Verify locally against seeded content (Sveltia local dev workflow)
  - [ ] Document GitHub fine-grained PAT setup for the artist's login
  - [ ] E2E smoke check that `/admin` serves and config parses
- [ ] 11. Define AWS infrastructure with Terraform
  - [ ] `infra/`: versioned private S3 bucket, CloudFront distribution with Origin Access Control, response-headers/caching policy, bucket policy limited to the distribution
  - [ ] IAM OIDC provider + least-privilege deploy role trusted by the GitHub repo (s3 sync + cloudfront invalidation only)
  - [ ] Outputs: bucket name, distribution ID, distribution URL
  - [ ] Validate: `terraform fmt`, `terraform validate`, and `terraform plan` (apply requires user's AWS credentials - user runs apply)
- [ ] 12. Create GitHub Actions workflows
  - [ ] `ci.yml`: lint + typecheck + unit tests (+ e2e) on pull requests
  - [ ] `deploy.yml`: on push to `main` - install, lint, test, build, assume OIDC role, sync `dist/` to S3 with correct cache headers, create CloudFront invalidation
  - [ ] Document required repo variables/secrets (AWS region, role ARN, bucket name)
- [ ] 13. Write documentation
  - [ ] Rewrite README: what it is, architecture overview, developer quickstart, one-time setup/deploy guide (Terraform apply, GitHub secrets, CMS/PAT setup)
  - [ ] Add `docs/ARTIST_GUIDE.md`: non-technical walkthrough - logging into /admin, editing each section (headshots, videos, awards, galleries), uploading images, embedding a video, changing colours/fonts, publishing expectations (1–2 min delay)
- [ ] 14. Final verification pass
  - [ ] Full suite green: lint, typecheck, unit tests, e2e, production build
  - [ ] Manual smoke checklist across all five routes (/, /actor, /musician, /about, /contact) at desktop + mobile widths, including carousel, lightbox and an embedded video
  - [ ] Confirm `npm run preview` serves the built site correctly (SPA fallback caveat noted below)

## Current Unknowns

- [ ] Unknown: editorial workflow behaviour with PAT authentication (draft branch naming, PR flow) - verify during task 9.
- [ ] Unknown: SPA client-side routing on CloudFront needs a custom error response mapping 403/404 → index.html; confirm behaviour in task 10 and document any limitations for deep links.
- [ ] Unknown: whether GitHub-hosted runners + S3 transfer costs stay within free tier for typical usage - expected yes for a portfolio-scale site; revisit if usage grows.
- [ ] Unknown: visual design direction beyond "clean, neutral, content-first" - v1 ships one tasteful default theme; artist adjusts via theme settings.

## Decisions

| Decision | Rationale |
|---|---|
| Single artist per deployment | User confirmed; avoids auth/multi-tenancy complexity; each artist deploys their own copy. |
| Git-based CMS (Sveltia) instead of a backend | User wants zero-backend ideally; Sveltia gives non-technical users a real editor UI while keeping infra to static hosting only. |
| Sveltia over Decap | Comparable schema/config model but better editor UX and no OAuth-proxy infrastructure required. |
| Sveltia auth via GitHub PAT ("Sign In with Token") | Verified against official docs: zero server-side setup; token page opens with required scopes pre-selected. Upgrade path exists (Sveltia CMS Authenticator OAuth client) if needed later. |
| S3 + CloudFront with Origin Access Control | User choice; cheap, fast, private origin; default CloudFront certificate for v1. |
| Terraform for IaC | User choice; repeatable infra in `infra/`. |
| GitHub Actions with OIDC deploy role | User choice; no long-lived AWS keys stored in GitHub. |
| Content imported at build time (static) | Site remains purely static and fast; publish delay accepted by user. |
| Vue 3 + TypeScript + Vite; vue-router only, no Pinia | User chose TS; app state is simple enough for composables - fewer dependencies. |
| Videos via YouTube/Vimeo embed URLs only | Avoids video hosting/storage entirely; matches "no backend" goal. |
| Custom lightweight lightbox | Avoids pulling a modal/UI library for one well-understood component. |
| Contact page = links + mailto in v1 | Keeps v1 backend-free; form backend (Lambda + SES) recorded as future enhancement. |
| Vitest + Vue Test Utils; Playwright for one e2e smoke spec | Standard Vue ecosystem tooling; e2e kept deliberately small. |
| CloudFront default URL in v1 | User choice; Route53/ACM added later behind existing Terraform. |
| Fixed page structure; artists edit content + theme only | Avoids rebuilding Squarespace. Layout lives in code; all customisation is data (content, colours, fonts, toggles). Future flexibility = config knobs on fixed slots, not composition. |
| Draft preview mode deferred to post-v1 | User decision: v1 ships without it. The designed approach (editorial workflow + `?preview=<branch>` fetching raw GitHub content, zero extra infra) is preserved in Future Enhancements for the follow-up session. |
| Single **public** repo holding both code and content | User wants the project visible as portfolio work. Repo content equals site content (already public); public repos get unlimited Actions minutes; the Sveltia PAT is required for writes regardless of visibility. Two-repo split still adds cross-repo checkout + deploy triggering for no v1 benefit; documented as a future migration if template-distribution ever happens. Known trade-off: post-v1 draft branches would be publicly browsable pre-publish. Never commit secrets - AWS access uses OIDC roles only. |
| Node upgraded to 24 LTS on dev machine (user-approved) | Latest stack requires it: vue-router 5 needs vite ≥7.3, and vite 7.3+/8 + jsdom 27+ require Node ≥22.12 (dev box had 22.8). CI runners already use current Node. |
| TypeScript pinned to ^6.0.x (not 7) | TS 7 is the new native compiler and removed `lib/tsc`; vue-tsc@3.3.11 cannot drive it yet (`ERR_PACKAGE_PATH_NOT_EXPORTED`). Revisit pin when a TS7-compatible vue-tsc ships. |
| ESLint 10 flat config: plugin-vue `flat/essential` + @vue/eslint-config-typescript; Prettier owns formatting | `flat/essential` avoids stylistic template rules that conflict with Prettier. Verified working combo: eslint@10.9, eslint-plugin-vue@10.10, @vue/eslint-config-typescript@14.9. |
| Latest majors adopted: vite@8, vitest@4, jsdom@30, vue-router@5, @playwright/test@1.62 | User requested latest compatible installs; full toolchain verified green on this set. Vitest config lives in separate `vitest.config.ts` sharing the `@` alias with `vite.config.ts`. |
| Content stored as JSON files (not Markdown) | Structured fields map 1:1 to Sveltia widgets and TS types; avoids adding a markdown parser dependency for v1's fixed layout. Long-form text is arrays of paragraph strings (`bioParagraphs`). |
| Content layer lives in `src/composables/content/` (types / defaults / normalize / loaders) | Keeps planned `composables/` structure; normalizers are pure functions over `unknown` so missing-file/missing-field fallbacks are unit-testable without mocking `import.meta.glob`. Loaders are non-reactive plain functions (content is static at build time). |
| Artwork slugs derived from filenames (`artworks/<slug>.json`) | Single source of truth, CMS-friendly (Sveltia uses filename as identifier); titles fall back to a prettified slug when absent. |
| Font pairing presets defined in the content type: `classic \| modern \| editorial \| playful` | Lets seed data validate against the real preset list now; task 3 maps each preset to actual font stacks/CSS variables. Unknown values fall back to `classic`. |
| Placeholder media committed as hand-made SVGs in `public/images/uploads/` | No offline source of real artwork photos; lightweight, diff-friendly, artist replaces them via the CMS upload flow. Seed contact email uses `.example` domain to avoid pointing at a real address. |
| **Redesign (2026-08-22): actor–musician dual portfolio** | User's final design: nav = Home · About Me · Contact only; Home shows two headshot tiles linking to `/actor` and `/musician`; generic Gallery route + artworks collection removed. Page skeletons stay hardcoded in Vue; all images/text come from CMS content with graceful fallbacks. |
| One Sveltia folder collection per page section (`actor/{videos,headshots,gallery}`, `musician/{highlights,projects,gallery}`) | Each editable section maps to exactly one CMS collection - clearest UX for a non-technical artist; filename becomes the slug, `order` field controls display sequence. Exception: the musician Awards block lives in `pages/musician.json` (single text + two fixed picture slots, no list to order). |
| Section headings live in page JSON with hardcoded code fallbacks | Structure is fixed in code but wording stays artist-editable; missing fields render sensible defaults ("Actor", "Gallery", "Awards", …). |
| Headshots = inline sideways carousel; gallery grids open an image-only lightbox | User choice. Videos are inline embeds in the new design, so the lightbox drops its planned video branch. |
| Existing placeholder SVGs partially reused; no new image files created | User instruction: reuse existing placeholders where they fit (portrait → headshots, scenic images → heroes/galleries), delete the rest, and never create SVGs unless explicitly asked. Seed content ships with some slots intentionally empty to exercise graceful-missing behaviour. |
| Themes are named presets; `content/settings/theme.json` stores only `{ "preset": "<name>" }` | User wants themes as selectable options. A code registry (`THEME_PRESETS`, keyed by name with display labels) is the single source of palettes + font pairings; unknown/missing selections fall back to the default preset. The same registry will drive the Sveltia select widget (task 10) and any future front-end switcher. Per-colour editing is deferred until needed. |
| "Chocolate Truffle" four-category palette: primary #713600, secondary #C05800, surface #FDFBD4, ink #38240D | User-supplied colours as four equal categories (Figma-style token grouping) instead of three roles + fixed text: primary = main brand (links/buttons/headings), secondary = accent/support (highlights, hovers, tags), surface = page/card backgrounds, ink = darkest (dark surfaces, strong emphasis). |
| Text colour auto-derived per category (`resolveOnColors`): palette-coherent candidate if it meets AA 4.5:1, else the higher-contrast of white/black | User requirement: "text changing to whatever fits that colour best". Result for chocolate truffle: on-primary = cream (8.7:1), on-secondary = black (4.6:1 - computed; black edges white 4.53:1 on caramel), on-surface = espresso (12.0:1), on-ink = cream (12.0:1). Every pairing is unit-tested ≥ AA. |
| Border colour derived from text at 15% alpha; each preset bundles a font pairing | Keeps presets to the four user-facing colours; `--color-border` = rgba(text, 0.15) so borders stay coherent in every future theme. "Chocolate truffle" ships with the `classic` pairing (serif headings / system sans body); other pairings already defined for future presets. |
| CSS variables applied via a `:style` binding on the app root element | Pure-function mapper (`themeToCssVariables`) keeps mapping unit-testable without DOM mutation; static build-time content needs no reactivity or watchers. Stylesheet `:root` defaults mirror the default preset so pre-hydration paint matches. |
| Route table exported as `routes` from `src/router/index.ts`; components/tests create their own routers from it | Lets component tests build isolated memory-history routers (no singleton state leakage between tests) while `main.ts` keeps the single default-exported web-history router; one source of truth for route definitions. |
| `toEmbedUrl()` converts watch/share/shorts URLs to privacy-enhanced embeds (`youtube-nocookie.com`, `player.vimeo.com`); unrecognised URLs render no iframe | Artists paste ordinary YouTube/Vimeo page links in the CMS; conversion happens at render time so content stays human-friendly. youtube-nocookie avoids tracking cookies; unknown URLs fail soft (empty slot) instead of rendering a broken iframe. Lives in `src/composables/video.ts`, shared with the Musician view. |
| Swiper adopted for the headshots carousel (user-requested premade solution, 2026-08-22 tweak pass) | User asked for something off-the-shelf to "flick through images"; Swiper is the Vue-ecosystem standard and bundles touch/swipe, keyboard nav, arrows and a11y that the hand-rolled carousel lacked. ~30 KB gz added to the lazy-loaded route chunk only. Interaction tests are delegated to the library; component tests assert slide rendering + conditional arrows. |
| Mobile nav opens a fullscreen overlay dialog rather than an inline dropdown (user request, 2026-08-22) | Same `<nav>` element restyled below 640px via `.site-nav--open` (fixed inset-0, centred links) so desktop keeps the plain inline row; gains `role="dialog"`/`aria-modal` only while open, body scroll lock, Escape-to-close, hamburger→X morph, focus moves to first link on open and back to the toggle on close. |
| Awards section redesigned (user request, 2026-08-22): one text block + two picture slots, replacing the per-award list | User confirmed the whole section becomes a single editable text chunk plus exactly two image slots; individual award titles disappear. Data lives on `MusicianPageContent` (`awardsText`, `awardsFirstImage`, `awardsSecondImage`) instead of a folder collection — nothing left to order, so no collection is created; `AwardEntry`/`normalizeAward`/`useAwards` removed and seed entries deleted. Missing picture slots render surface fallback blocks; both empty hides the grid entirely. |
| About/Contact headings are CMS-editable fields with hardcoded defaults (`aboutHeading`, `contactHeading`) | Extends the existing "section headings live in page JSON" decision to the remaining two views so every visible page title stays artist-editable; defaults "About Me"/"Contact". E2e nav expectation updated from h1 "About" to "About Me". |
| Contact page renders `note ?? "Messages go directly to the artist's email inbox."` | Plan requires UI copy explaining replies reach the artist's email even before the artist writes their own note; CMS note overrides the default line rather than duplicating it. |
| Social icons are inline SVGs in a local `SocialIcon.vue`, keyed by hostname via `resolveSocialIconKind()` (no icon-library dependency) | User asked for icons instead of text links; the dependency list stays minimal (`vue`, `vue-router`, `swiper`). Outline-style 24×24 currentColor glyphs cover instagram/youtube/x/facebook/tiktok/spotify/spotlight with a generic link-icon fallback so any future platform still renders sensibly. |
| One social-links DOM list inside `<nav>`, repositioned by CSS: absolutely top-right of the header bar ≥640px, static bottom row in the mobile overlay modal | User placement request ("top right in navbar and bottom in mobile modal"); keeping a single source of truth avoids duplicated link lists drifting apart, and the icons automatically inherit the nav's dialog semantics while the menu is open. Auto-margin flex layout pins socials to the modal bottom while the links stay visually centred. |
| Contact gains an optional `phone` field rendered as a `tel:` link (whitespace/hyphens/parens stripped from the href) | User request to add "a space for a phonenumber"; optional so artists without a public number omit it cleanly. Seed uses Ofcom's fictional `+44 7700 900123` range like the `.example` email. |

## Progress Log

| Date | Event |
|---|---|
| 2026-08-22 | Plan created. |
| 2026-08-22 | Verified Sveltia CMS auth via official docs (PAT sign-in, no OAuth server needed); resolved auth unknown. |
| 2026-08-22 | Added draft preview mode (editorial workflow + `?preview=<branch>`) after scope discussion; recorded target user (single artist, "Max"). |
| 2026-08-22 | Deferred draft preview mode to post-v1 per user decision; moved to new Future Enhancements section. |
| 2026-08-22 | Decided single private repo for code + content; two-repo split recorded as future migration path. |
| 2026-08-22 | Flipped to single **public** repo per user preference (portfolio visibility); trade-offs recorded. |
| 2026-08-22 | Task 1 complete. Scaffolded Vite + Vue 3 + TS app in repo root: ESLint 10 flat config + Prettier + vue-tsc typecheck; Vitest 4 (jsdom) with router unit tests; Playwright skeleton with 2 passing e2e specs; vue-router 5 with placeholder routes for Home/Gallery/About/Contact; base stylesheet + planned directory structure (`src/components`, `src/composables`, `content/*`, `public/images/uploads`). User approved Node upgrade to 24.19 LTS during install resolution; TS pinned to ^6.0.3 (vue-tsc lacks TS7 support). All verification green: dev server HTTP 200, lint, typecheck, unit tests (2 passed), e2e (2 passed), production build. |
| 2026-08-22 | Task 2 complete. Built content layer in `src/composables/content/`: TS types for settings/theme/pages/artworks, pure normalizers with per-field fallback defaults, and eager `import.meta.glob('/content/**/*.json')` loaders (`useSiteSettings`, `useTheme`, `useHomePage`, `useAboutPage`, `useContactPage`, `useArtworks` sorted by `order`, `useArtwork(slug)`). Seeded fictional artist "Max Rivera": site/theme/home/about/contact JSON + 6 artworks incl. one YouTube video entry; 8 SVG placeholder images in `public/images/uploads/`. 26 new unit tests cover normalizer fallbacks and seeded-content loading through the real glob path. Verification green: lint, typecheck, unit tests (28 passed), production build. Note: content module is tree-shaken out of the current bundle because no view consumes it yet - views wire up in tasks 5–8; glob inlining is verified by the Vitest run which uses the same Vite transform pipeline. |
| 2026-08-22 | Design locked with user (Q&A): actor–musician dual portfolio. Nav = Home/About Me/Contact only; Home = two headshot tiles → `/actor` + `/musician`; Actor page = hero, videos, headshots carousel, portrait gallery; Musician page = hero, intro, awards, highlights, original projects, gallery; cross-links between both. Generic Gallery route + artworks collection removed; lightbox becomes image-only (videos inline); headshot carousel stays inline. Plan restructured: task 2b added, tasks 4–14 rewritten/renumbered, Overview gained a "Site structure" section, 5 new decisions recorded. |
| 2026-08-22 | Task 2b complete (user approved immediate implementation). Content layer rebuilt for the new design: types/defaults/normalizers/loaders for `useHomePage` (headshot slots), `useActorPage`, `useActorVideos`, `useHeadshots`, `useActorGallery`, `useMusicianPage`, `useAwards`, `useHighlights`, `useProjects`, `useMusicianGallery`; shared generic `loadEntries(dir, normalize)` sorts by explicit `order`. Router: `/gallery` removed, `/actor` + `/musician` added with placeholder views; nav placeholder = Home/About Me/Contact; e2e spec updated to match (not run). Content reseeded as an actor-musician persona across 7 new folder collections (14 entry files); placeholder SVGs reused where they fit (portrait → headshots, scenic → heroes/galleries), `artwork-studio-process.svg` deleted, no new images created. 36 content tests + router tests green. Verification: lint ✓, typecheck ✓, unit tests 38 passed, production build ✓ (all five view chunks emitted). |
| 2026-08-22 | Task 3 complete. Theme system implemented as a named-preset registry: `src/composables/theme/` (types, presets, resolver/mapper) - `THEME_PRESETS` currently holds one preset, "chocolate truffle" (#713600 primary, #C05800 accent, #FDFBD4 background, #38240D text, classic font pairing); `content/settings/theme.json` now stores `{ "preset": "chocolate truffle" }` and unknown/missing selections fall back to the default preset. `themeToCssVariables()` maps palette+fonts to the seven app CSS custom properties (border derived as rgba(text, 0.15)) and is bound to the app root in App.vue; `:root` stylesheet defaults mirror the default preset. WCAG contrast utilities added and asserted in tests. Verified: lint ✓, typecheck ✓, unit tests 47 passed (9 new theme tests), production build ✓ with theme confirmed present in the emitted bundle. Future Enhancements gained "front-end theme switcher". |
| 2026-08-22 | Palette model revised per user correction: the four colours are four equal categories (primary / secondary / surface / ink) rather than three roles + fixed text. Text per category auto-derived via `resolveOnColors()` - palette-coherent candidate when it meets AA 4.5:1, otherwise higher-contrast of white/black. Computed result for chocolate truffle: on-primary cream, on-secondary **black** (4.64:1 beats white's 4.53:1 on caramel - initial white assumption corrected by the contrast math), on-surface espresso, on-ink cream. Emits 8 semantic vars (`--color-primary/secondary/surface/ink` + `--color-on-*`) plus legacy `--color-bg/text` aliases so existing CSS keeps working; border = rgba(ink, 0.15). Verified: lint ✓, typecheck ✓, unit tests 50 passed, build ✓. |
| 2026-08-22 | Task 4 complete. Built shared layout: `SiteHeader.vue` (site name from settings linking home; nav = Home/About Me/Contact only; active-route styling via `router-link-exact-active`; hamburger toggle below 640px with `aria-expanded`/`aria-controls`, menu closes on link click and on route change) and `SiteFooter.vue` (social links with `rel="noopener noreferrer"` + © year/name). Both consume `useSiteSettings()` directly. `App.vue` now composes header/main/footer in a flex column shell (sticky footer); `.visually-hidden` utility added globally. Router route table exported as `routes` so tests build isolated memory-history routers (decision logged). 8 new component tests (nav links, active class, toggle, auto-collapse, footer socials/copyright) read real seeded content through the actual glob path - e2e link names unchanged so existing specs stay valid (not run per plan note). Verification: lint ✓, typecheck ✓, unit tests 58 passed (+8), production build ✓. |
| 2026-08-22 | Task 5 complete. `HomeView.vue` rebuilt from placeholder: artist name (`h1`) + tagline from site settings, two side-by-side portrait-ratio headshot tiles linking to `/actor` and `/musician`, each with a label; missing image slots render a styled fallback block instead of an `<img>` (graceful-missing behaviour). 4 component tests in `src/views/__tests__/HomeView.spec.ts` mock `useHomePage` over the real module (settings stay real seeded data) to cover CMS-driven images, partial-missing, and full-missing cases. E2e home spec updated for the new `h1` ("Max Rivera" replaces placeholder "Home"; not run per plan note). Verification: lint ✓, typecheck ✓, unit tests 62 passed (+4), production build ✓ with a dedicated HomeView chunk. |
| 2026-08-22 | Task 6 complete. `ActorView.vue` rebuilt: full-viewport hero (`100svh`, cover image or surface fallback) → "Actor" heading with video articles (title/`VideoEmbed`/description) → "Gallery" heading with Headshots carousel + Gallery Images portrait grid → "Also a musician" cross-link to `/musician`. New reusable pieces: `VideoEmbed.vue` (16:9 iframe wrapper, renders nothing without a resolvable URL) and `toEmbedUrl()` in `src/composables/video.ts` converting YouTube watch/share/shorts/embed + Vimeo links to privacy-enhanced embed URLs (6 unit tests; decision logged). Carousel: translateX track, prev/next buttons disabled at ends, visible counter with `aria-live`, `aria-roledescription="carousel"`, arrow-key support via keydown on the group; controls hidden for single/empty lists; missing slide images get fallback blocks. 7 ActorView component tests mount against real seeded content by default (loaders wrapped via `vi.fn(actual)` + `mockReset()` restores real impls) with overrides for multi-slide navigation and empty-content graceful handling. Note: seed gallery titles use em-dashes ("As Prospero - The Tempest") - PowerShell console output had masked this, first test run caught the mismatch and expectations were corrected. Verification: lint ✓, typecheck ✓, unit tests 75 passed (+13), production build ✓ with dedicated ActorView chunks. |
| 2026-08-22 | Actor view tweak pass per user feedback. Headshots carousel replaced with **Swiper** (`swiper/vue`, Navigation + Keyboard + A11y modules) for real flick/swipe support - dependency addition recorded as a decision; arrows render only with >1 slide. Layout changes: "Actor"/"Gallery" are now large centred titles (`clamp(2.25rem–3.5rem)`); videos span full page width with description centred beneath the embed and top titles removed; headshot carousel centred (42rem cap) directly under the Gallery title acting as its highlight; "Gallery Images" subheading removed from the grid; gallery grid upsized to 1 col → 2 cols ≥640px → max 3 cols ≥1024px. Content layer: unused `headshotsHeading`/`imagesHeading` fields dropped from `ActorPageContent` (type/default/normalizer/seed/tests) so the CMS never exposes dead knobs. Component tests updated (ResizeObserver stubbed for Swiper in jsdom; slide-count + conditional-arrows assertions replace hand-rolled nav tests). Verification: lint ✓, typecheck ✓, unit tests 76 passed, production build ✓ (ActorView chunk lazy-loaded, ~30 KB gz Swiper cost). |
| 2026-08-22 | Width tweak per user feedback ("too much buffer on the sides"): added a `.page--wide` modifier (max-width 90rem vs 72rem default) applied to the Actor videos and gallery sections; headshot Swiper widened 42rem → 60rem. Tests made seed-count agnostic after discovering `content/actor/headshots/main-headshot copy.json`, an exact duplicate of `main-headshot.json` that appeared outside the session - left in place (user content; delete if unintended), single-vs-multi carousel cases now mocked explicitly instead of relying on seed counts. Verification: lint ✓, typecheck ✓, unit tests 77 passed, production build ✓. |
| 2026-08-22 | Unified content width per user request ("make it all the same wide"): global `.page` container now 90rem for every view; the `.page--wide` modifier removed again along with its ActorView usages; Home headshot tiles no longer capped at their own 56rem; header/footer inner containers widened to match (90rem). User confirmed `main-headshot copy.json` is an intentional duplicate added to exercise carousel scrolling - kept, and count-agnostic tests stay valid. Verification: lint ✓, typecheck ✓, unit tests 77 passed, production build ✓. |
| 2026-08-22 | User hand-tuned widths to a preferred 90rem and reworked the header bar (absolutely centred site name, left-aligned nav, right-side toggle) - preserved as-is. Mobile nav rebuilt per user request into a **fullscreen overlay dialog**: below 640px the same `<nav>` becomes fixed inset-0 with centred large links; `role="dialog"` + `aria-modal` bound only while open, body scroll locked while open, Escape closes, hamburger morphs to an X, focus moves to the first link on open and returns to the toggle on close. Four new header tests (dialog semantics, scroll lock, Escape close; existing toggle/route-change/link-click kept green). Two seed-drift test failures surfaced: seeded video URLs now use YouTube ID `L_LUpnjgPso` - expectations aligned in video + ActorView specs. Verification: lint ✓, typecheck ✓, unit tests 80 passed (+4), production build ✓. |
| 2026-08-22 | Responsive tweak: Home headshot tiles collapse from 2 columns to 1 below 640px (matches the app's existing mobile breakpoint). Verification: lint ✓, typecheck ✓, unit tests 80 passed, production build ✓. |
| 2026-08-22 | Home tile labels ("Actor"/"Musician") now overlay their images instead of sitting beneath: absolute bottom-centred heading text in white with text-shadow over a bottom gradient scrim (predictable legibility over any uploaded photo); hover/focus switches to a white underline (primary-colour hover removed as unreadable over photos). Verification: lint ✓, typecheck ✓, unit tests 80 passed, production build ✓. |
| 2026-08-22 | Task 7 complete (implemented in a prior working session, committed by user as `2983ceb lightbox`, verified this session). `ImageLightbox.vue`: teleported fullscreen overlay dialog (`role="dialog"`, `aria-modal`), prev/next with wrap-around, caption + "n / total" counter (`aria-live`), arrow-key navigation, Escape and backdrop-click close, close button, body scroll lock, focus moved in on mount and restored on unmount, image-only (videos stay inline per decision), fallback block for missing images. Wired into ActorView's gallery grid (buttons with `cursor: zoom-in` open it; entries without images render fallbacks and no trigger). 11 component tests. Verification: lint ✓, typecheck ✓, unit tests 94 passed, production build ✓. |
| 2026-08-22 | Task 8 complete. `MusicianView.vue` rebuilt from placeholder, mirroring ActorView patterns: fullscreen `100svh` hero (cover image or surface fallback) → "Musician" heading + centred intro → Awards (title + text per entry, optional image above) → Highlights and Original Projects (`VideoEmbed` articles with centred descriptions, no top titles) → Gallery (portrait grid, description below each image, images open the shared `ImageLightbox` with descriptions as captions) → "Actor →" cross-link. All five section headings always render from page JSON; only lists are conditional on content (matches ActorView's graceful-missing behaviour). 13 component tests mount against real seeded content with loader mocks over the real module (hero/headings/intro/awards ± image, highlights, projects, gallery + lightbox open/close, no-trigger-without-image, actor cross-link, empty-sections graceful case). Recurring gotcha re-confirmed: PowerShell console masks em-dashes - seeded award title "OffCommendation - Best Supporting Performance" uses a real U+2014; byte-level check settled it and the expectation was corrected. Verification: lint ✓, typecheck ✓, unit tests 107 passed (+13), production build ✓ with a dedicated lazy-loaded MusicianView chunk. |
| 2026-08-22 | Awards section redesigned per user request ("one chunk of text and two pictures"; whole-section interpretation confirmed via Q&A - the per-award list is gone entirely). Content model: `AwardEntry`/`normalizeAward`/`useAwards` and the `content/musician/awards/` collection deleted; `MusicianPageContent` gained `awardsText` + `awardsFirstImage`/`awardsSecondImage` (optional, trimmed). Seed: both former award texts merged into one paragraph on `pages/musician.json`; picture slots reuse the matched pair `artwork-tide-lines-i/ii.svg` (all placeholders already in use; reuse precedent exists via duplicated portrait.svg), awards folder removed. View: centred text block + two side-by-side 3/4 picture slots with surface fallbacks per missing slot; grid hidden when both empty. Tests: loader spec lost the two award tests and gained normalizer + seeded-slot coverage; MusicianView spec now 14 tests (text+pictures from CMS, single-slot fallback, fully-empty hide, plus prior coverage). Also aligned seed text to the user's ASCII-hyphen convention (commit cd8ce85 had normalized the old em-dash seeds). Verification: lint ✓, typecheck ✓, unit tests 108 passed, production build ✓. |
| 2026-08-22 | Task 9 complete. About/Contact views rebuilt from placeholder. Content model: `AboutPageContent` gained `aboutHeading`, `ContactPageContent` gained `contactHeading` (defaults "About Me"/"Contact", CMS-editable per the headings-in-page-JSON decision); seeds updated. **About**: large centred title, portrait (3/4, bordered, fallback block when missing) beside the bio paragraphs ≥640px / stacked below, optional statement rendered as a centred italic serif quote block with a top border on desktop. **Contact**: centred title, pill-shaped primary-colour "Enquire by email" mailto button (secondary-colour hover using on-secondary text), the email address repeated as a mailto link below, social links list (target=_blank + rel=noopener noreferrer, hidden when none), note copy = CMS `note ?? "Messages go directly to the artist's email inbox."`. E2e nav expectation updated h1 "About" → "About Me" (not run per plan note). 10 new component tests (5 per view: seeded rendering, statement/note presence + fallbacks, missing-portrait fallback, safe social links, empty-socials case). Verification: lint ✓, typecheck ✓, unit tests 118 passed (+10), production build ✓ with dedicated lazy-loaded About/Contact chunks. All five routes now have real views; only tasks 10-14 remain. |
| 2026-08-22 | Header socials + contact phone per user request ("add a space for a phonenumber... 3 social links to the navbar and modal... use icons... top right in navbar and bottom in mobile modal"). New `SocialIcon.vue` (inline outline SVGs, currentColor) + `resolveSocialIconKind()` hostname resolver (instagram/youtube/x/twitter/facebook/tiktok/spotify/spotlight → glyphs, unknown → generic link icon; 3 unit tests). `SiteHeader.vue`: nav links wrapped in `.site-nav__links`, seeded socials (Instagram/YouTube/Spotlight) rendered once inside `<nav>` as icon links (aria-label, target=_blank, rel=noopener noreferrer) - absolutely positioned top-right of the header bar on desktop, static bottom row inside the fullscreen modal via auto-margin flex (links stay centred, icons enlarge to 1.75rem). Contact: optional `phone` field (type/default/normalizer/seed `+44 7700 900123` Ofcom fictional) rendered as a `tel:` link with separators stripped from the href; hidden when unset. Tests +4 header (icon links, modal placement), +2 contact (tel link, absent-phone case), +1 loader (seeded phone). Verification: lint ✓, typecheck ✓, unit tests 125 passed (+7), production build ✓. |
